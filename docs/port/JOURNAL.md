# Surfe Port Journal

本日志只追加已执行任务的事实与证据。任务状态以 `STATE.json` 为唯一事实来源。

## INIT — 初始化 surfe-port 持久化控制器

- 日期：2026-08-13。
- 状态：完成。
- Surfe 源码：未克隆、未构建、未建立 reference/oracle、未执行 T00；仅为固定计划的文件/符号映射核对了公开冻结提交的 `surfe_lib`、`math_lib` 和 `test` 目录及相关声明。
- 修改文件：`.agents/skills/surfe-port/SKILL.md`、`.agents/skills/surfe-port/agents/openai.yaml`、`.gitignore`、`docs/port/PLAN.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。
- 核心实现：无。仅建立显式触发的仓库级 Skill、T00–T34 固定计划、跨会话 JSON 状态、追加式日志和 reference/oracle 忽略规则。
- 验证命令：Skill `quick_validate.py`；JSON 解析和状态一致性检查；YAML frontmatter 与 `openai.yaml` policy 检查；T00–T34 连续性和每任务必备字段检查；`git diff --check`；变更范围检查。
- 验证结果：全部通过；`next_task` 为 `T00`，未产生算法代码、正式 golden fixture、C++ 源码或 T00 交付物。
- Parity 证据：不适用；INIT 不执行 parity。
- 性能证据：不适用；INIT 不进行性能判断。
- 后续发现：无；下一任务固定为 T00。

## T00 — 冻结 Surfe reference、许可证、迁移范围、兼容策略和纯 Rust 边界

- 日期：2026-08-13。
- 状态：完成。
- Surfe reference：从官方仓库克隆到被忽略的 `.cache/surfe-reference`；detached HEAD 与 `git rev-parse HEAD` 均确认 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：`License.txt@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`README.md@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`.gitmodules@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`CMakeLists.txt@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`surfe_lib/surfe_api.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Surfe_API::ComputeInterpolant`、`EvaluateInterpolantAtPoint(s)`、`EvaluateVectorInterpolantAtPoint(s)` 和约束/参数/状态入口，以及 `surfe_lib/debug.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `open_console_window` 排除证据。
- 修改文件：`docs/port/source-baseline.md`、`docs/port/compatibility.md`、`docs/port/licensing-and-rust-boundary.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。
- 核心实现：固定上游仓库/提交与 reference 三段优先级；记录 `License.txt` SHA-256 `9fd4e80cac11aa3d00278e4d2634ff0c6a169303014155f51873f9c4e6e6441f`、Crown Copyright/MIT notice 义务和逐模块来源追踪规则；对冻结仓库顶级路径给出纳入、证据限定或排除判定；固定有效输入、数值/离散 parity、缺陷和类型化错误策略；列出生产、测试和发布的全部纯 Rust/native 禁止项。未建立 oracle、fixture 或算法实现。
- 验证命令：`git -C .cache/surfe-reference rev-parse HEAD` 与 reference clean-status 检查；工作树及 `git show <commit>:License.txt` 双重 SHA-256/许可证关键文本校验；`git check-ignore`；固定提交、reference 优先级、排除项、兼容规则和全部 native 禁止项的 `rg` 机器检查；tracked C/C++/二进制拒绝检查；T00 状态/任务序列 `jq`/`diff` 校验；`git diff --check`。
- 验证结果：全部通过；reference 提交精确匹配且无本地修改，reference/oracle 路径被忽略，三份交付文档的必需字段齐全，GeoRBF 未跟踪 Surfe 源码、对象、库或可执行文件。
- Parity 证据：T00 只完成不可漂移的 source/license/scope 判定基线；固定提交和许可证内容已逐字节验证。按计划尚未构建 T03 oracle、T04 fixture 或执行数值 parity，未将其误报为通过。
- 性能证据：不适用；T00 不进行性能判断或性能声明。
- 后续发现：`Surfe_API::SetTangentConstraints` 调用 planar 添加入口、`Surfe_API(int)` 未显式初始化全部状态布尔值、`SetRegressionSmoothing`/`SetGreedyAlgorithm` 无条件启用等源码观察已登记到既有 T02/T30/T31 核验范围；未创建任务或提前修复。下一任务固定为 T01。

## T01 — 建立完整 C++ 清单、调用链、数据流和依赖图

- 日期：2026-08-13。
- 状态：完成。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 再次确认 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树无修改。
- 阅读的 Surfe 源码：完整审计 `surfe_lib/*.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`math_lib/*.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`surfe_pybindings/pybindings.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的核心适配调用、`test/main.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4`，并读取根 `CMakeLists.txt@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 以固定 Eigen/OpenMP/pybind11 与可选 Qt/VTK/geo_builder 边界。重点逐体核对 `Surfe_API` 两个工厂和拟合/评估入口、`GRBF_Modelling_Methods::{get_method,setup_basis_functions,run_greedy_algorithm}`、五模型的全部虚函数覆写、`RBFKernel`/`Modified_Kernel` 组合、三个 solver 与 `Math_methods` QP 路径。
- 修改文件：`docs/port/inventory.md`、`docs/port/call-graph.md`、`docs/port/data-flow.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。
- 核心实现：无 Rust 算法。建立 30 个范围文件的逐文件清单、类型/字段/逐符号清单、五模型和 12 个纯虚槽覆写矩阵；记录 API/Greedy 双工厂、从约束到分组/pair/layout/matrix/RHS/weights/scalar/gradient 的字段级流、LU/QP/LOQO/重建路径、裸指针所有权，以及 Eigen/CMake/OpenMP/pybind11/Qt/VTK/geo_builder 的迁移/替代/排除判定。
- 验证命令：`git -C .cache/surfe-reference rev-parse HEAD` 与 clean-status；`find` 枚举范围文件并用 `rg -F` 将每个 `path@commit` 与文档双向核对；`rg` 提取 class/struct/enum；Perl 去注释后提取限定符号并以 `rg` 核对清单；表驱动检查五模型在公开工厂中的分支和每个模型头中的 12 个覆写；检查三份交付物包含调用者、被调用者、状态所有者、全部 layout、依赖处置和后续任务归属；`git diff --check` 与 staged 变更范围检查。
- 验证结果：全部 T01 门槛通过；28 个核心 `.h/.cpp` 加 pybindings/test 两个范围文件均有准确提交路径，源码类型、枚举、去注释限定符号、五个公开工厂分支和 60 个模型覆写槽核对无缺口；三份文档明确入口、调用关系、字段所有者及每项依赖的迁移/Rust 替代/排除判定。额外的非门槛 C++ 编译探针因 reference 的 Eigen submodule 未初始化而未运行，未把它记录为构建通过；T01 的固定验收不要求构建，T03 oracle 构建仍必须独立取得实测证据。
- Parity 证据：T01 的离散 parity 证据仅为冻结声明/定义/调用点与清单的集合核对；未建立 T03 oracle、T04 fixture 或数值 golden，未宣称数值 parity。
- 性能证据：不适用；T01 不实现或基准测试算法，未作性能判断。
- 后续发现：公开 API 工厂为五个显式分支，而 `GRBF_Modelling_Methods::get_method` 的 Greedy 工厂无显式 Vector Field 分支；若干内联常量体、抛出体、仅声明符号、`SetTangentConstraints` 的实际调用目标、批量状态检查/共享可变核与状态位初始化等事实已映射到既有 T02/T30/T31，未提前分类或修复。下一任务固定为 T02。

## T02 — 逐符号能力分类

- 日期：2026-08-13。
- 状态：完成。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整复核 `surfe_lib/*.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`math_lib/*.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`surfe_pybindings/pybindings.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 和 `test/main.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的声明、定义与活动调用点。重点逐体核对 `basis.cpp` 的 15 类核方法、`modeling_methods.cpp` 的 `GRBF_Modelling_Methods::{check_input_data,run_greedy_algorithm}`、五模型 60 个覆写槽、三个完整 `convert_modified_kernel_to_rbf_kernel`、`continuous_property.cpp` 全部覆写、`surfe_api.cpp` 的状态/setter/批量入口、`modelling_input.cpp` 的空输入与 Greedy residual selector，以及 `grbf_exceptions.h` 的 23 个具体错误类别和 `SurfeExceptions` wrapper。
- 修改文件：`docs/port/symbol-classification.md`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。
- 核心实现：无 Rust 或 C++ 算法实现。为 T01 清单建立互斥的 `I/P/T/U/D/V/X` 逐符号分类，每项记录定义/调用证据、可观察缺口和唯一既有任务归属；把源级缺陷同步到兼容策略，未修改算法、未新增任务。
- 验证命令：`git -C .cache/surfe-reference rev-parse HEAD/status --short`；`find` 核对 30 个范围文件；Perl 去注释限定符号提取与 `rg` 反向检索；从 T01 清单提取代码标识符并反向检索分类表；表驱动核对 13 个完整核的 208 个 `.cpp` 公式和 13 个内联 clone、`R/AR` 的 30 个哨兵导数符号、五模型 60 个覆写槽；`rg` 搜索 TODO/空体/恒定返回/throw/调用点；`jq` 状态序列校验；`git diff --check`。
- 验证结果：全部通过。源级集合核对覆盖 501 个 T01 代码标识符 token 和 432 个去注释限定定义/调用 token；完整核共 221 个公式/clone 符号，`R/AR` 为 24 个字面 `throw -666` 体加 6 个间接别名；`run_greedy_algorithm` 只有声明/定义文件，三个完整 reconstruction 函数零调用，`use_greedy` 的五处命中均是声明、入参、默认值或写入，没有消费点。
- Parity 证据：T02 只固定源码能力/不可达/缺陷分类；未建立或运行 T03 C++ oracle，未生成 T04 golden，未宣称数值 parity。
- 性能证据：不适用；T02 不实现或基准测试算法，未作性能判断。
- 后续发现：`Parameters::use_greedy` 和 `GRBF_Modelling_Methods::run_greedy_algorithm` 不可达归 T31；Single/Lajaunie/Stratigraphic 的完整 reconstruction 函数不可达归 T21；状态、约束路由、越界和共享可变 kernel 问题分别归既有 T26/T27/T29/T30/T31。T01 文本的“22 个异常”是计数笔误，实际列出 23 个；计划提示的 `Point::operator==` 在冻结声明/定义中不存在，真实同点符号是 `collocated`。以上均映射到既有任务或清单更正，未创建新任务。下一任务固定为 T03。

## T03 — 建立仓库外冻结 Surfe reference oracle

- 日期：2026-08-13。
- 状态：完成。
- Surfe reference：按既定优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。为外部构建初始化冻结 gitlink `eigen-git-mirror@36b95962756c1fce8e29b1f8bc45967f30773c00`；首次网络传输中断后 Git 自动重试成功，最终 submodule HEAD 与 gitlink 精确一致。
- 阅读的 Surfe 源码：`surfe_lib/surfe_api.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的约束 setter、`ComputeInterpolant`、scalar/vector 单点与批量入口及错误状态；`surfe_lib/basis.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `RBFKernel::{basis,dx_p1,dx_p2,dy_p1,dy_p2,dz_p1,dz_p2,dxx..dzz,get_global_anisotropy,scaled_radius,basis_*}`、`Lagrangian_Polynomial_Basis` 和 `Modified_Kernel::basis_*`；`surfe_lib/matrix_solver.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 LU/QP/LOQO 输入、验证、`solve` 和 `weights`；`surfe_lib/modeling_methods.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的清洗、level/group/reference、basis、equality matrix 和所有权；`surfe_lib/{single_surface,lajaunie,stratigraphic_surfaces,continuous_property,vector_field}.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `process_input_data/get_method_parameters/get_interpolation_matrix/get_*values/setup_system_solver/eval_*` 及 increment/层序提取点；必要依赖 `surfe_lib/modelling_input.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`math_lib/math_methods.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`；以及 `test/main.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的调用顺序和可视化排除证据。
- 修改文件：`docs/port/oracle-protocol.md`、`docs/port/oracle-manifest.json`、`tests/common/oracle_protocol.rs`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。外部 `.cache/surfe-oracle/oracle_smoke.cpp`、request/response、Rust test binary 和 C++ executable 均被忽略且未提交。
- 核心实现：固定 `georbf-surfe-oracle` v1 UTF-8 JSON Lines envelope、commit 身份、成功/错误不变量、binary64/binary32/non-finite 编码、row-major matrix/vector 表示、稳定错误结构和确定性环境；定义 `identity`、`kernel.evaluate`、`model.run`、`solver.run`、`error.probe`，覆盖核值、两点一阶导数、混合 Hessian、anisotropy、Modified Kernel、五模型约束/分组/layout/matrix/RHS、LU/QP/LOQO 求解证据、scalar/gradient 和错误。新增不发现或启动 oracle 的纯标准库 Rust envelope 校验辅助；正常构建仍不依赖 reference。
- 外部构建与 smoke：使用 `g++ 13.3.0`、`-std=c++11 -O2 -DNDEBUG -fopenmp` 和冻结 Eigen，编译冻结 `math_lib/surfe_lib` 与被忽略 adapter。`model.run/single_surface_linear_smoke` 经 `Surfe_API::ComputeInterpolant` 运行四个 interface、一个 planar、Cubic、一阶 polynomial，并与直接模型矩阵证据路径交叉核对；输出 11×11 matrix、11 项 RHS/weights、LU 残差和 scalar/gradient。adapter/source、请求、响应和 executable 的最终 SHA-256 分别为 `4e3f56fabd073f7ed489cceaf3f04ee4df30dbb9169207ec7767efa76ffd956a`、`cd1a994c0d075f3cc001c5a1518b4b691eb0eca13bcbbc0370385b26043cb267`、`b15aaba2405d51278d61d1d4b0d5e57c0c4fcb2ae1adc1cecabfbd067321cc7c`、`cd9815aaf26a60d725883bb2a1dff88f24c313ef1b94ce1a9685cece6c399d11`。
- 验证命令：reference HEAD/clean-status 与 Eigen gitlink/HEAD 检查；上述 `g++` 构建；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续运行相同请求并 `cmp`；`jq` 校验 request/response envelope、commit、成功/错误 schema、11×11/121 项 matrix、11 项 RHS/weights、有限性、solver 分支与 prediction；`rustc --test tests/common/oracle_protocol.rs` 并把真实 smoke 行显式传入；manifest `jq` 覆盖检查；`git check-ignore`、tracked native 扩展拒绝检查、`git status --ignored`；状态连续性检查；`git diff --check`。
- 验证结果：全部 T03 门槛通过。两次成功响应逐字节相同且 SHA-256 均为 `b15aaba2405d51278d61d1d4b0d5e57c0c4fcb2ae1adc1cecabfbd067321cc7c`；response commit 精确匹配，所有数值有限，LU `attempted/success` 为真，`residual_l2 = 0`，公开 API prediction 为 scalar `0.5`、gradient `[0,0,1]`；实际 invalid request 返回 request-stage `invalid_request`；Rust 4 个协议测试全部通过；仓库未跟踪任何 C/C++/Eigen/object/library/executable/response。
- Parity 证据：T03 证明冻结 oracle 可构建、公开 API 与中间 matrix/RHS/solve/prediction 可在同一协议响应中交叉提取，且同输入输出字节稳定；它不生成正式 golden、不定义 T04 容差，也不宣称 T32 全局 parity。smoke 的公开 API 与直接模型路径在 `1e-14` 内一致，实测结果恰好相同。
- 性能证据：不适用；T03 只验证 oracle 可运行和确定性，未执行同机 Surfe/GeoRBF benchmark，未作性能声明。
- 后续发现：构建再次触发冻结 `modelling_input.cpp` tangent residual selector 的“non-void function control reaches end”警告，与 T02 已登记的不可达缺陷一致，仍归 T31，不新建任务、不修改 reference。下一任务固定为 T04。

## T04 — 定义 golden fixture 与差分测试协议

- 日期：2026-08-13。
- 状态：完成。恢复上下文时确认仓库根目录没有 `AGENTS.md`，按控制器规则记录后继续；任务开始前工作区干净，当前分支为 `main`。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink 仍为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：`surfe_lib/basis.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `RBFKernel::{basis,dx_p1,dx_p2,dy_p1,dy_p2,dz_p1,dz_p2,dxx,dxy,dxz,dyx,dyy,dyz,dzx,dzy,dzz,get_global_anisotropy,scaled_radius,basis_*}`、九个 isotropic 核、anisotropic 核和 `Modified_Kernel::basis_*`；`surfe_lib/matrix_solver.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `System_Solver`、`Linear_LU_decomposition::solve`、`Quadratic_Predictor_Corrector::solve`、`Quadratic_Predictor_Corrector_LOQO::solve` 及验证入口；`surfe_lib/{single_surface,lajaunie,stratigraphic_surfaces,continuous_property,vector_field}.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `get_interpolation_matrix`、`get_equality_values`、可达 `get_inequality_matrix/get_inequality_values`、`setup_system_solver`、`eval_scalar_interpolant_at_point` 和 `eval_vector_interpolant_at_point`。
- 修改文件：`docs/port/fixtures.md`、`tests/fixtures/schema/golden-fixture-v1.schema.json`、三个 `tests/fixtures/schema/*` 合成正反例、`tests/common/parity/mod.rs`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。
- 核心实现：定义 `georbf-surfe-golden` v1 envelope、冻结 source/oracle 身份、生成 argv/环境/平台/精度和 request/response 行 SHA-256；固定默认 exact、唯一 numeric path 分类和 12 个分层 binary64/binary32 容差；固定 signed zero、tagged NaN/Inf、row-major matrix/vector 及错误编码；以精确有序坐标、方向、levels、evaluation points、显式 LU/QP/LOQO matrix/RHS 固定九核、两点一阶导数、混合 Hessian、anisotropy、Modified Kernel、五模型、matrix/RHS、scalar/gradient、objective/iteration/error 的确定性 case family；规定非唯一权重只作诊断，以机器可读 residual/feasibility ceiling 配合实际尝试、有限性、目标和 witness predictions 共同判定 pass/fail。新增零依赖 Rust JSON reader/canonical serializer、fixture validator、matrix/vector invariant、数值/路径 comparator；未实现迁移算法，未提交正式数值 golden。
- 验证命令：reference HEAD/clean-status/gitlink 检查；`rustfmt --edition 2021 --check tests/common/parity/mod.rs`；`rustc --edition=2021 -D warnings --test tests/common/parity/mod.rs` 后运行；Python `jsonschema` Draft 2020-12 schema 自校验和一个正例/两个反例；`jq -c` 与 `sha256sum` 复核 schema 正例内 request/response 行 hash；固定环境下用 T03 外部 oracle 对相同 smoke request 连续运行两次并 `cmp`；coverage/name `rg` 检查；`git check-ignore`；tracked native/正式 golden 拒绝检查；状态连续性检查；`git diff --check`。仓库尚无 `Cargo.toml`，因此计划所述条件性 `cargo test` 不适用，未伪造为运行通过。
- 验证结果：Rust 7 个测试全部通过，覆盖 schema 正反例、规范序列化往返、容差闭边界及下一 binary64 值失败、signed zero、tagged non-finite、路径唯一分类、分层阈值和 matrix/vector 长度/编码；Draft 2020-12 schema 有效，`valid-minimal.json` 通过，source 漂移和 `all_numeric` 单一宽容差反例均被拒绝；正例 hash 与内嵌值一致。冻结 oracle 两次响应逐字节相同且 SHA-256 均为 `b15aaba2405d51278d61d1d4b0d5e57c0c4fcb2ae1adc1cecabfbd067321cc7c`，source commit/status 复核通过；所有提交外 reference/oracle 文件保持 ignored。
- Parity 证据：T04 固定了后续差分的可审计格式、离散 exact 规则、分层数值判定和确定性覆盖矩阵，并以真实冻结 oracle 重跑证明同输入输出字节稳定；schema 目录中的三个 JSON 只为合成结构测试，不是正式数值 fixture。本任务未宣称任何 Rust 数学实现、模型或 T32 全局数值 parity 已通过。
- 性能证据：不适用；T04 不实现算法或运行同机 Surfe/GeoRBF benchmark，未作性能判断。
- 后续发现：T05 建立 Cargo 工程后应把现有零依赖 parity helper 纳入普通 `cargo test`，但不得让正常测试发现或启动 oracle；该工作已属于固定 T05，不新增任务。下一任务固定为 T05。

## T05 — 审查 GeoRBF 并建立纯 Rust 工程护栏

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。`STATE.json` 的 INIT–T04 连续前缀、`last_completed_task=T04`、`active_task=null`、`next_task=T05` 校验一致后才激活 T05。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：`.gitmodules@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 Eigen/pybind11 gitlink；`CMakeLists.txt@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 C++11、OpenMP、Eigen3、共享 `math_lib/surfe_lib`、pybind11 和条件性 Qt5/VTK/geo_builder 分支（冻结提交不存在计划中的条件性 `surfe_lib/CMakeLists.txt`）；`surfe_lib/surfe_lib_module.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `SURFE_LIB_EXPORT/SURFE_LIB_NO_EXPORT/SURFE_LIB_DEPRECATED*` 及 `surfe_lib/surfe_api.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 DLL 导出类；`math_lib/math_methods.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`surfe_lib/{basis,matrix_solver,modeling_methods,single_surface,lajaunie,stratigraphic_surfaces,continuous_property,vector_field,surfe_api}.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 和 `surfe_pybindings/pybindings.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 Eigen `Matrix3f/MatrixXd/VectorXd/Vector3d`、`SelfAdjointEigenSolver`、`partialPivLu` 及矩阵/向量 API 使用点。
- 修改文件：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`.github/workflows/ci.yml`、`.gitignore`、`tools/audit_pure_rust.py`、`tests/protocols.rs`、`tests/test_pure_rust_guard.py`、`tests/common/parity/mod.rs`、`docs/port/georbf-audit.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。
- 核心实现：先为 Cargo 接入和护栏写失败测试，再建立 edition 2021、MSRV 1.82 的单 package library crate；生产/dev/build 依赖与 feature 均为空，`unsafe_code` 为 forbid，现有 oracle/parity helper 通过 `tests/protocols.rs` 进入正常 Cargo test 且仍不发现/启动 reference。新增全标准库 Python 护栏：审计 all-feature 完整 Cargo metadata graph、package family、所有 `custom-build` target、Cargo `links`、直接/传递 Rust 源码 native ABI/`#[link]`、全部 manifest、tracked/untracked 非 ignored 路径和 `cargo package --list`，拒绝 C/C++、Eigen、Qt、VTK、CMake、bindgen、cxx、pybind11、BLAS/OpenBLAS/LAPACK/MKL、build script、native 制品及 reference/oracle。CI 在 Linux/macOS/Windows 矩阵配置 fmt、严格 clippy、全部 test/doc、护栏、tree 和 package list；没有提前实现任何数学或模型功能。
- 验证命令：`rustc/cargo/rustfmt/clippy --version`；reference HEAD/clean-status 和 `rg` Eigen/export 使用点；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；workflow YAML parse；状态连续性与变更范围检查；`git diff --check`。提交后的干净工作树再运行精确 `cargo package --list --locked`。
- 验证结果：本地 `rustc/cargo 1.85.0` 下全部通过。严格 clippy 无 warning；Cargo 运行 11 个 protocol/fixture 测试全部通过，lib/doc test 为 0 个且成功；6 个 Python 护栏测试覆盖全部冻结禁止 family、custom build、links、native FFI、native/package/reference 路径及防误报；实际审计输出 `pure-Rust audit passed`。metadata/tree 只有 `georbf@0.1.0` 一个 package/resolve node，依赖为零、`links=null`、无 custom-build；release build 与严格 rustdoc 成功。package allowlist 为 13 个文件，不含 C/C++、reference/oracle、build script 或审计/CI 工具；试打包和从包验证构建成功，大小 68.3 KiB、压缩后 13.6 KiB。workflow YAML 可解析；没有把未发生的远端三平台 run 伪记为 CI 通过，实际平台证据仍归 T34。
- Parity 证据：本任务不实现或比较 Surfe 数学行为；T03/T04 的 4 个 oracle protocol 测试与 7 个 fixture/parity 基础测试现已在普通 `cargo test` 中全部通过，且测试不访问 oracle。未生成正式 golden，未宣称 T32 数值 parity。
- 性能证据：不适用；T05 没有算法或同机 Surfe/GeoRBF benchmark，未作性能判断。
- 后续发现：没有新增任务。T06 起新增 Rust 模块和任何经证明必要的纯 Rust 依赖都必须继续通过 T05 护栏；下一任务固定为 T06。

## T06 — 参数、枚举、默认值、名称和类型化错误

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。`STATE.json` 的 INIT–T05 连续前缀、`last_completed_task=T05`、`active_task=null`、`next_task=T06` 校验一致后才激活 T06。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整读取 `surfe_lib/modelling_parameters.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `D2R/R2D/Epilson`、`Parameter_Types::{DWRT,SecondDerivatives,FirstDerivatives,RBF,SolverType,ModelType,AXIS}`、`Parameters/InternalParameters/InputParameters`；完整读取 `surfe_lib/grbf_exceptions.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 23 个具体异常、`SurfeExceptions` 和 `GRBF_Exceptions` 常量；完整读取 `surfe_lib/surfe_api.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`，重点核对 `get_method_from_parameters`、两个构造函数、`SetRegressionSmoothing`、`SetGreedyAlgorithm`、`SetRestrictedRange`、两个 `SetRBFKernel`、shape/polynomial/anisotropy setter 和可观察错误分支；补充读取 `surfe_pybindings/pybindings.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的整数模型构造和字符串核 setter 暴露、`surfe_lib/modeling_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的普通/anisotropic 核枚举分支与内部模型工厂，以及五模型构造体的参数复制路径。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/parameters.rs`、`src/error.rs`、`tests/parameters_compat.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t06_parameters_probe.cpp` 和 `.cache/surfe-oracle/t06-parameters-probe` 仅作冻结 C++ oracle，不进入生产、测试依赖或发布包。
- 核心实现：新增纯标准库参数模块，保持七组枚举声明判别顺序、三个参数结构逐字段默认、三项 binary64 常量、九个区分大小写/空格的精确 RBF 名称、五个 C++ 枚举文本形式，以及与枚举后三项顺序不同的公开整数模型代码 `1,2,3,4,5 -> Single,Lajaunie,Vector,Stratigraphic,Continuous`。参数 setter 保留冻结源码的写入语义，包括 smoothing/Greedy 忽略 bool 并写 `true`；非法核名不改变原值。新增 `#[non_exhaustive] Error`，逐项映射 23 个 C++ 异常类、精确 `what()` 诊断文本和 `SurfeExceptions` 嵌套格式；程序匹配使用类型化类别而非文本。所有值由安全 Rust 所有权承载，无 C++ ABI、raw pointer、算法、约束、核或求解实现。
- 测试优先证据：先加入 `tests/parameters_compat.rs`，首次运行按预期因根模块缺少全部 T06 类型而编译失败；实现后 8 个 T06 表驱动测试通过，覆盖所有判别、默认、常量 bit pattern、9 个合法/11 个非法核名、5 个模型枚举文本、5 个合法/5 个非法整数代码、setter 写入、23 个异常与 wrapper 格式。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部非可视化核心源码编译被忽略探针；探针直接验证实际工厂动态类型、setter 后中间参数、默认/枚举、核名接受与拒绝、非法名称不变性、异常文本和嵌套 wrapper。固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出相同：`t06-oracle defaults=ok enums=ok rbf_accept=9 rbf_reject=11 model_accept=5 model_reject=3 errors=23 wrapper=ok`，输出 SHA-256 为 `a5347d02650e6b740f0b247344e5990bc35391ceec92523ecede8cf9f768c620`；probe source/binary SHA-256 分别为 `f10ba23d825921252bf95cbd5208f4fc1409567598ccde8db04219ac23d8df4c`、`e8be49c3e8a13651044eff615f4d472cb7daefe44524abce008bd6524e02098e`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次确定性运行；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --doc --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native 文件拒绝、ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T06 门槛通过。严格 Clippy、rustdoc 和格式检查无 warning；Cargo 共运行 19 个测试（8 个 T06、11 个协议/fixture）且全通过，doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`；打包清单 16 个文件，包含 T06 源码和兼容测试、不含 C++/reference/oracle，试打包与从包构建成功（97.2 KiB，压缩后 20.2 KiB）。
- Parity 证据：冻结 C++ 探针与 Rust 测试对相同离散事实分别验证，默认、常量 bit pattern、枚举、工厂映射、严格名称接受/拒绝、setter 写入、23 个错误类别及诊断文本均一致。模型字符串只作为精确 C++ 枚举标识符的 Rust 文本往返，文档明确冻结 Surfe 没有模型字符串公开入口；未扩大 Surfe 兼容输入声明。没有生成 T32 正式 fixture，也未宣称数学或全局 parity。
- 性能证据：不适用；T06 仅实现常数时间的离散参数/错误映射，不执行数学算法或同机 Surfe/GeoRBF benchmark，未作性能判断。
- 后续发现：冻结 `grbf_exceptions.h` 使用 `std::string` 却未自包含 `<string>`；oracle 探针按正常依赖包含顺序补齐标准头，未修改 reference 或复制该编译卫生缺陷。C++ 构建再次出现已登记到 T31 的 tangent residual selector 漏返回警告，没有新建任务。下一任务固定为 T07。

## T07 — 约束数据类型与方向转换

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。`STATE.json` 的 INIT–T06 连续前缀、`last_completed_task=T06`、`active_task=null`、`next_task=T07` 校验一致后才激活 T07。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink/HEAD 均为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：完整读取 `surfe_lib/modelling_input.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`，逐体核对 `Point`、`Interface`、`Inequality`、`Planar`、`Tangent` 的字段、构造和访问器，`Planar::{_compute_strike_dip_polarity_from_normal,_compute_normal_from_strike_dip_polarity,getDipVector,getStrikeVector,setNormalBounds}` 以及 `Tangent::setAngleBounds`；补充读取 `surfe_lib/surfe_api.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `AddPlanarConstraintw{Normal,StrikeDipPolarity,AzimuthDipPolarity}` 与其他 Add 入口，并以 `docs/InputsAndOutputs.md@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 核对公开 polarity 只接受 0/1、azimuth 是 dip direction。完整文件中属于 T09/T31 的空间和 Greedy 函数只作边界核对，未提前实现。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/geometry.rs`、`src/constraints.rs`、`tests/constraints_geometry.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t07_geometry_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖安全值类型 `Point/Interface/Inequality/Planar/Tangent/Constraints` 和独立 `ConstraintError`；初始化并拥有坐标、`c`、level、scalar/vector field，使用 `Option` 代替 C++ 设置前未初始化的 normal/angle bounds。逐运算保持 `acos/atan2` normal→dip/strike/polarity、strike/dip/polarity→normal、azimuth 两分支、dip/strike vector、四角 normal envelope 和 Tangent angle bounds；保留传入非单位方向的原始尺度，不隐式归一化。安全拒绝非有限量、零 normal/tangent、`nz` 超出 `acos` 域、退化方向和 0/1 以外 polarity。未实现 residual/Greedy、reconstruction setter、排序、同点、空间算法、矩阵或 fitted model。
- 测试优先证据：先加入 `tests/constraints_geometry.rs`，首次运行按预期因根模块缺少全部 T07 类型而编译失败；实现后 9 个 T07 测试通过，覆盖 Point 状态、Interface/Inequality、normal 六个轴向极点与四象限、非单位输入不归一化、upright/overturned、strike/dip/azimuth 及 90° 分支边界、round trip、单位/正交恒等式、normal/angle bounds、插入顺序和全部安全拒绝类别。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部非可视化核心编译被忽略探针；首次命令因把 Eigen include 写成旧目录名而失败，改为实际冻结 gitlink `eigen-git-mirror` 后构建成功。固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出逐字节相同，覆盖 Point/Interface 默认与写入、normal 极点/象限/非单位分量、两种 polarity、dip/strike vector、normal bounds、Tangent bounds、azimuth 两分支及 90° 边界，也记录 C++ 接受零方向/Inf 和 `nz>1` 产生 NaN 的无效输入行为。最终 probe source、binary、输出 SHA-256 分别为 `5b164ba4f33a680be3788ef3bbc5460711cf76b67072453c67d36069a620ae28`、`46f0192c0522a0a4fdf58cd07c134c67c45de36bf73d529945a582aec7553338`、`ef8989f95623a7df3f45a788975b15a67e1545881ca67cf996a5358287cc63ce`。
- 验证命令：reference HEAD/clean-status 和 Eigen gitlink/HEAD；上述 C++ 构建及两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T07 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 28 个测试（9 个 T07、8 个 T06、11 个协议/fixture）且全通过，doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`；打包清单 19 个文件，包含 T07 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（126.1 KiB，压缩后 25.9 KiB）。
- Parity 证据：Rust 测试对冻结探针的同组 hexfloat golden 做数值比较，Point/level/default/bounds 的离散状态、polarity、azimuth 分支和 category/order 均精确，方向、向量和 bounds 全部通过 `2e-14` 尺度阈值；round trip、单位长度和 dip/strike 正交恒等式同时通过。零/非有限输入差异仅为已文档化安全拒绝，不改变有效输入结果；未宣称 T32 全局 parity。
- 性能证据：不适用；T07 仅实现常数规模值构造和方向转换，未运行 T33 同机模型 benchmark，未作全局性能声明。
- 后续发现：冻结 C++ 全核心构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning，未修改 reference、未新增任务。下一任务固定为 T08。

## T08 — 排序、同点判定、去重与 level/reference 分组

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。`STATE.json` 的 INIT–T07 连续前缀、`last_completed_task=T07`、`active_task=null`、`next_task=T08` 校验一致后才激活 T08。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/modelling_input.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Point::operator<`、实际同点自由函数 `collocated`、`Point` 派生约束 payload 和 `Constraints` 四类别（冻结源码不存在计划提示的 `Point::operator==`）；完整核对 `surfe_lib/modeling_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `GRBF_Modelling_Methods::{_get_distinct_interface_iso_values,_get_interface_points,_get_distinct_inequality_iso_values,get_interface_data,remove_collocated_constraints}` 及 `modeling_methods.h` 的 level/group/reference 容器；完整核对 `math_lib/math_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Math_methods::sort_vector_w_index` 和头中声明/SWAP；补充读取 `surfe_lib/surfe_api.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::ComputeInterpolant` 的清洗调用顺序、`lajaunie.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::_get_increment_pairs` 与 `stratigraphic_surfaces.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::_get_increment_pairs` 的同层/相邻 reference 自由度消费点；未实现这些后续 pair 或模型路径。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/ordering.rs`、`src/constraints.rs`、`src/constraints/grouping.rs`、`tests/ordering_grouping.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t08_ordering_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增有限坐标的 x/y/z 字典序适配（signed zero 等价、稳定保持精确等价项输入顺序）、逐轴严格 `< 1e-3` 的 `collocated`、四类别独立排序及相邻去重和分类移除计数；逐控制流安全移植 Numerical Recipes indexed sort 的 7 项 insertion threshold、partition/stack 调度、重复/signed-zero 顺序和长度失败；新增 exact level 降序去重、每 level 首点 reference 索引、移除 singleton 后的多点组索引，以及同层 `sum(len-1)` 和相邻 reference `len-1` 前置计数。没有使用 hash 迭代、容差 level、全局聚类，也没有提前实现空间、increment pair、layout 或矩阵。
- 测试优先证据：先加入 `tests/ordering_grouping.rs`，首次运行按预期因根模块缺少 `collocated/compare_points/sort_values_with_indices` 和 `Constraints` 分组/去重方法而编译失败；实现后 5 个 T08 测试通过，覆盖排序/index 两算法分支、`-0.0`、`1e-3` 前一 ULP/恰在边界/后一 ULP、非传递相邻去重链、同/跨类别重复、payload survivor、相邻 binary64 level、精确 level/reference/group、singleton、自由度计数、空输入和反转排列。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部非可视化核心编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出逐字节相同。探针确认 boundary `1e-3` 不同点、两侧分支、x/y/z 排序、indexed insertion/partition 的重复和 signed-zero 索引、长度失败、四类别独立去重、5 个精确降序 level、5 个 reference、3 个二点 multi group、3 个同层自由度及空接口失败。最终 probe source、binary、输出 SHA-256 分别为 `400b96d008f7368fe2dc0870b6ded529ffafffcf2886ac231c9ea878d3e4ed09`、`5d9e073169e8d6c3350c440ca94049cb3e44f2602ef6e45cb4f622323fe5b2f3`、`f02bae1a32fbdae1642a59c3686012f372dbd43a7617bdaf1d45eb9adb8731e4`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T08 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 33 个测试（5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`；打包清单 22 个文件，包含 T08 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（146.1 KiB，压缩后 30.7 KiB）。
- Parity 证据：Rust 测试逐项固定同一冻结探针的离散输出：边界谓词、value/index 顺序、每类 survivor、level bit pattern、reference/group 索引和自由度计数均精确一致；排列反转在不同坐标数据上得到相同清洗后分组。C++ `std::sort` 对精确比较等价项顺序未作标准保证，探针观察到首项保留；Rust 稳定适配将该观察定义为跨平台确定行为，兼容文档已明确记录。未生成 T32 正式 fixture，也未宣称模型数值或全局 parity。
- 性能证据：T08 仅实现前处理离散算法；运行了 release build，但未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：冻结全核心构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning；`Point::operator==` 计划提示与冻结源码不符的事实已在 T02 登记并由 T08 兼容文档关闭。没有新增任务；下一任务固定为 T09。

## T09 — 空间辅助算法

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T08 连续前缀、`last_completed_task=T08`、`active_task=null`、`next_task=T09` 一致后才激活 T09。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整读取 `surfe_lib/modelling_input.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`，逐体核对 `Constraints::compute_{inequality,interface,planar,tangent}_avg_nn_distance`、`compute_avg_nn_distances`、`SpatialParameters`、`convert_constraints_to_points`、`distance_btw_pts`、`nearest_neighbour_index`、`get_n_nearest_neighbours_to_point`、两个 `furtherest_neighbour_index`、`avg_nn_distance`、`spatial_metrics`、`Find_STL_Vector_Indices_FurtherestTwoPoints`、`Find_STL_Vector_Index_ofPointClosestToOtherPointWithinDistance`、`calculate_bounds`、`get_extremal_point_data_indices_from_points`、`is_index_in_list`、`get_largest_distance_between_points` 和 `get_maximal_axial_variability_order`；同时完整读取该文件中四个 residual selector 以确认边界。补充检索 `surfe_api.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::GetDataBoundsAndResolution`、`single_surface.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`lajaunie.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`stratigraphic_surfaces.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 和 `modeling_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的实际调用点；未提前实现模型或 Greedy 路径。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/spatial.rs`、`tests/spatial.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t09_spatial_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖纯 Rust 空间模块，保持 inequality/interface/planar/tangent 转点顺序、包含 `c` 的四维距离、严格比较产生的首个 tie-break、零距离最近邻过滤、T08 indexed sort 的多最近邻/极值/轴序离散顺序、不同索引可取得零距离的平均最近邻、collocation 清洗后 resolution/bounds、最远点对/目标距离点、最大距离和四类约束平均值。`SpatialParameters` 保持字段含义；冻结可变平均距离缓存改为拥有所有权的结果快照。新增 `SpatialError`，对空集合、少于两点、无非零邻居、非有限显式输入和 indexed sort 失败安全分类；`get_n_nearest_neighbours_to_point` 在零距离过滤后只返回实际候选，避免冻结源码的越界。
- 测试优先证据：先加入 `tests/spatial.rs`，首次 `cargo test --test spatial` 按预期因根模块缺少全部 T09 函数、`SpatialError` 和 `Constraints::compute_*` 方法而编译失败；实现后 7 个 T09 测试通过，覆盖 4D 手算距离、转换顺序、最近/最远/目标距离并列索引、零距离过滤、平均距离、分类平均、collocation metrics、bounds、最远点对、最大距离、extremal、轴序、平移/旋转性质，以及空集、单点、重复点、退化轴和非有限参数。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部非可视化核心编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出逐字节相同。探针确认 `sqrt(13)` 四维距离、最近/最远/目标距离首索引 0、`nearest_two=[0,1]`、空/单点平均为 0、重复平均为 `4/3`、metrics resolution 1、最远 pair `[1,2]`、重复 pair `[0,0]`、extremal `[0,1,2,3,4,5]`、并列轴序 `X,Z,Y`/全退化 `Z,Y,X`、最大距离 10、四类平均 `[4,4,0,0]` 和约束转点顺序。probe source、binary、输出 SHA-256 分别为 `8f4a9a64f9f772b0b7697c86c70551c06acc37127e4d72296f575f78dad71986`、`85a775fa1755f9a60e36de0c385a5d909c615dd322831d99c0af65e693c5e697`、`da2b7b442058e0e750bef84f822c45c8b5e8b0381db484c1087788dab250b8a9`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T09 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 40 个测试（7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布 allowlist 加入 `tests/spatial.rs` 后打包清单为 24 个文件，不含 C++/reference/oracle，试打包与从包构建成功（170.9 KiB，压缩后 35.6 KiB）。
- Parity 证据：Rust 测试对同组冻结探针离散结果和 hexfloat 数值逐项固定，所有有效输入索引、bounds、axis、转换顺序和空间量一致；手算距离、平移/旋转不变量同时通过。冻结 `spatial_metrics({})` 虽返回 `true`，却输出零 resolution 和 `DBL_MAX/-DBL_MAX` 哨兵 bounds；Rust 对这一无效输入返回 `EmptyPointSet`，兼容文档已记录。未生成 T32 正式 fixture，也未宣称模型或全局 parity。
- 性能证据：T09 保持冻结稠密辅助算法的线性/二次遍历并通过 release build；未运行 T33 同机模型 benchmark，未作全局性能判断。
- 后续发现：四个 `Get_*_STL_Vector_Indices_With_Large_Residuals` 只作为 T31 Greedy 候选选择消费本任务距离/平均距离；`continuous_property.cpp` 的 extremal 调用被注释且不可达。冻结全核心构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning。以上均登记到既有 T31，不新增任务；下一任务固定为 T10。

## T10 — Polynomial 与 truncated polynomial

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T09 连续前缀、`last_completed_task=T09`、`active_task=null`、`next_task=T10` 一致后才激活 T10。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/basis.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Polynomial_Basis`、`Poly_Zero/Poly_First/Poly_Second::{basis,dx,dy,dz,clone}` 及完整/截断分支；逐体读取 `surfe_lib/single_surface.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`surfe_lib/lajaunie.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`surfe_lib/stratigraphic_surfaces.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`surfe_lib/continuous_property.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `create_polynomial_basis`、`_get_polynomial_matrix_block`、`get_method_parameters` 和 polynomial 评估消费点，并核对 `vector_field.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 固定 `poly_term=false/n_poly_terms=0` 的边界；没有提前装配模型矩阵。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/polynomial.rs`、`tests/polynomial.rs`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t10_polynomial_probe.cpp` 和 `.cache/surfe-oracle/t10-polynomial-probe` 只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖 `PolynomialOrder::{Zero,First,Second}` 和安全值对象 `PolynomialBasis`；完整项序严格保持 `[1]`、`[x,y,z,1]`、`[x²,y²,z²,xy,xz,yz,x,y,z,1]`，x/y/z 一阶导数逐项同序。`truncated` 只删除末尾常数项，因此三个阶次长度分别为 0、3、9；显式保留 Lajaunie/Stratigraphic 所需语义。接口只读安全 `Point` 的 x/y/z，冻结 `c` 坐标不参与 polynomial；未引入高阶项、矩阵装配、Lagrangian basis 或后续模型 API。
- 测试优先证据：先加入 `tests/polynomial.rs`，首次 `cargo test --test polynomial` 按预期因根模块缺少 `PolynomialBasis/PolynomialOrder` 而编译失败；实现后 4 个 T10 测试通过，覆盖三阶完整/截断值与 x/y/z 导数、精确项序和 1/4/10 与 0/3/9 维数、原点/signed zero、负坐标、多尺度、忽略 `c`，并对六种 basis 逐项执行中央有限差分。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部非可视化核心编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续运行，两个输出 SHA-256 均为 `42bfdeb1f4a6b5504b65cee51fbb97cce4fd97bd32d3205c275265e2d1c3e6b5`。探针逐项输出三阶完整/截断 basis 与一阶导数以及负坐标/多尺度 hexfloat，确认维数、项序、常数截取和数值与 Rust 测试相同。probe source/binary SHA-256 分别为 `2b8b42fefb673381aaae5743adbf31f1c47fa095ee30fea491e0c86e625977c2`、`d3bb517335c4008104b90c43fe132ce98dfec084f3182c602ff0ad09a90f85aa`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建、两次固定环境运行和 SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T10 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 44 个测试（4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布 allowlist 加入 T10 源码/测试后打包清单为 26 个文件，不含 C++/reference/oracle，试打包与从包构建成功（181.0 KiB，压缩后 37.4 KiB）。
- Parity 证据：Rust exact 预期与冻结探针的同组完整/截断向量逐项一致；解析导数同时通过中央有限差分，完成计划要求的 C++ golden、解析和数值三角验证。有效输入的值、导数、项序、维数与截断均已实证；未生成 T32 正式 fixture，也未宣称模型或全局 parity。
- 性能证据：T10 为最多 10 项的常数规模直接求值并通过 release build；未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：Single Surface 使用完整 1/4/10 项；Lajaunie 使用截断 0/3/9 项；Stratigraphic reconstruction 固定一阶截断 3 项；Continuous Property 与 Vector Field 当前可达参数路径关闭 polynomial。其模型 layout/装配/实际可达性仍分别归既有 T16/T17/T21/T22/T25/T26/T27/T28，不新增任务、不提前实现。冻结全核心构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning。下一任务固定为 T11。

## T11 — Unisolvent 选择与 Lagrangian polynomial basis

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T10 连续前缀、`last_completed_task=T10`、`active_task=null`、`next_task=T11` 一致后才激活 T11。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/basis.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Lagrangian_Polynomial_Basis::{Lagrangian_Polynomial_Basis,_get_unisolvent_subset,_initialize_basis,poly,poly_dx,poly_dy,poly_dz}`、`_polynomial_constants/_derivative_polynomial_constants/unisolvent_subset_points` 和 `Modified_Kernel` 的构造消费边界；核对 `surfe_lib/grbf_exceptions.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4::failurecreatinglagrangianpolynomialbasis`；补充读取 `math_lib/math_methods.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4::sort_vector_w_index`、`surfe_lib/modelling_input.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Point/Interface` 和 `surfe_lib/modelling_parameters.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Epilson`，以固定排序、复制、索引与轴平面回退语义；未实现 T12 核或 T14 Modified Kernel。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/polynomial.rs`、`src/polynomial/lagrangian.rs`、`tests/lagrangian.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t11_lagrangian_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖 `LagrangianPolynomialBasis`，选择第一个严格最大点数组，复用 T08 冻结 indexed sort，逐控制流保留三个独立最大 range `if`、主轴两端/次轴排除端点后的两端、原始组内索引证据、轴常量平面的输入序替换与首个所选副本精确 `1e-3` 扰动。四组 `[constant,x,y,z]` 系数和求值/常量导数使用冻结显式行列式公式与运算顺序；只在行列式或系数为零/非有限时映射既有 `Error::LagrangianBasisCreationFailure`，任意非零有限近退化行列式均继续尝试，不按条件数或幅值预拒绝。
- 测试优先证据：先加入 `tests/lagrangian.rs` 和发布 allowlist，首次 `cargo test --test lagrangian` 按预期因根模块缺少 `LagrangianPolynomialBasis` 而编译失败；实现后 6 个 T11 测试在 debug/release 均通过，覆盖最大组与等大小首组、选择索引、输入反转、basis 系数与求值、Kronecker 恒等式、解析/中央有限差分导数、轴平面扰动、空/不足、最大 range 并列、斜共面、共线、重复和 `1e-12` 近退化。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部非可视化核心编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出逐字节相同。探针确认普通选择 `[0,1,2,3]`、反向输入 `[5,4,3,2]`、轴平面首点 x 从 `2` 变为 `2.001`、普通与近退化值/三向导数、Kronecker 值、空/不足/最大轴并列的同类异常；同时记录斜共面、共线和重复点在冻结构造后产生非有限 basis，Rust 按兼容策略安全拒绝。最终 probe source、binary、输出 SHA-256 分别为 `eba8c31c1040f7d20e686e4da7c2d72e95b9a604307e106ec688a1e6c4523d7d`、`9255795930d7764c5405dfd4bf122f177412c9226767a7817f473c41a01e6881`、`10b9a6ebbc564a0e2f2f62161f716c7870eafa1f6f97c1463043f433b6497c99`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test lagrangian`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T11 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 50 个测试（6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T11 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 28 个文件，包含 T11 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（203.4 KiB，压缩后 42.1 KiB）。
- Parity 证据：Rust 普通和近退化测试逐位固定同组 C++ hexfloat 的 basis 值与三向导数，选择几何、普通/反向原始索引、最大 range 并列错误和轴平面 `1e-3` 分支一致；所选四点的 Kronecker 恒等式及解析/有限差分导数同时通过。冻结斜共面/共线/重复输入的非有限结果先由 probe 记录，再在兼容文档明确为安全类型化拒绝；不改变非零有限行列式的数学结果。未生成 T32 正式 fixture，也未宣称 Modified Kernel 或全局 parity。
- 性能证据：T11 保持冻结的 indexed sort 选择和常数规模四函数直接求值，并通过 release build/release 测试；未运行 T33 同机模型 benchmark，未作全局性能判断。
- 后续发现：Lagrangian 所选点和系数已与 Modified Kernel 解耦验证，其 Value/Planar/Tangent 消去组合仍严格归既有 T14；冻结全核心构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning。没有新增任务；下一任务固定为 T12。

## T12 — 九种 isotropic 核及导数

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T11 连续前缀、`last_completed_task=T11`、`active_task=null`、`next_task=T12` 一致后才激活 T12。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/basis.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Kernel/RBFKernel` 字段与 `RBFKernel::{radius,basis_pt_pt,basis_pt_planar_x,basis_planar_x_pt,basis_pt_planar_y,basis_planar_y_pt,basis_pt_planar_z,basis_planar_z_pt,basis_planar_planar}`，以及 `Cubic/Gaussian/MQ/MQ3/TPS/IMQ/R/WendlandC2/MaternC4::{basis,dx_p1,dx_p2,dy_p1,dy_p2,dz_p1,dz_p2,dxx,dxy,dxz,dyx,dyy,dyz,dzx,dzy,dzz,clone}` 的全部声明与定义；补充核对 `surfe_lib/modeling_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::GRBF_Modelling_Methods::{setup_basis_functions,create_rbf_kernel}` 的九分支普通工厂与 shape/cutoff 传递。各向异性体只用于确认 T13 边界，Modified Kernel/泛函仍分别留给 T14/T15。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/kernel/mod.rs`、`src/kernel/derivatives.rs`、`src/kernel/isotropic.rs`、`tests/isotropic_kernels.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t12_isotropic_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖纯 Rust `IsotropicKernel`，统一承载九种冻结普通核、原样 shape/support 参数、含 `c` 的四维 radius、对第一/第二点的 x/y/z 一阶导数、完整 row-by-column 3×3 mixed Hessian、`SecondDerivative` 精确派发和一次性 `KernelEvaluation`。八个有完整源码体的核逐表达式保留运算、符号、零距和 Wendland 严格 `radius > cutoff` 分支；`R::basis` 保留半径值，而 12 个直接 `throw -666` 与三个间接别名安全映射为 `KernelError::LinearDerivativeUnavailable`，没有臆造线性核导数。TPS/Wendland 对非零 `c_delta` 的源码 Hessian 缺项先由 oracle 记录，再按源码保持并写入兼容文档；标准公开 `c1=c2=0` 路径同时满足解析和数值导数。
- 测试优先证据：先加入 `tests/isotropic_kernels.rs` 和发布 allowlist，首次 `cargo test --test isotropic_kernels` 按预期因根模块缺少 `IsotropicKernel/KernelError` 而编译失败；实现后 9 个 T12 测试在 debug/release 均通过，覆盖九核 value、八核两点导数和 81 个核/分量 Hessian 槽、enum 派发、交换/符号/Hessian 对称、两点中央有限差分、零距、`2^-20` 邻域、`1e-3/1/1e3` 多尺度、Wendland oblique interior/支撑内外边界、非零 `c_delta` 源码特例和 `R` 类型化错误。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen、`basis.cpp` 和 `math_methods.cpp` 编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次生成 40 行输出并逐字节相同。探针覆盖九核 separated/zero/near、非零 `c_delta`、Wendland 支撑内/边界/外及 oblique interior；确认 `R` value 正常且所有导数进入整数 `-666`。最终 probe source、binary、输出 SHA-256 分别为 `84bfc0f806740c9872f9c147d9c98072c8b17392e6d238f20440c3af703207bb`、`726d2f629808dcecfae770c26d769c04a546df5dd514aca8d6d25ed671a1824a`、`7655bf1c76411a201482b3505d90bc585e745139bc31af4c602c943441166096`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test isotropic_kernels`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T12 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 59 个测试（9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T12 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 32 个文件，包含 T12 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（239.5 KiB，压缩后 48.4 KiB）。
- Parity 证据：标准 separated 数据上八个完整导数核的 basis、两组一阶导数和九项 Hessian 共 16 个 binary64 结果逐位匹配冻结 hexfloat，`R` basis 也逐位匹配且导数错误分支一致；Wendland oblique interior 的 16 项和非零 `c_delta` TPS 的 16 项另作逐位回归，零距/near/支撑离散分支与探针一致。八核解析径向交换、导数反号、Hessian 对称及两点有限差分全部通过；`R` 无源码导数、TPS/Wendland 非零 `c_delta` 数学冲突已显式记录，未伪造三角验证。未生成 T32 正式 fixture，也未宣称各向异性、Modified Kernel 或全局 parity。
- 性能证据：T12 保持每次核/导数常数时间直接求值，并通过 release build 与任务专属 release 测试；未运行 T33 同机模型 benchmark，未作全局性能判断。
- 后续发现：各向异性普通核的 `f32 Matrix3f` 变换和导数仍严格归既有 T13；Modified Kernel 与 Value/Planar/Tangent 全组合仍归 T14/T15。没有新增任务；下一任务固定为 T13。

## T13 — 全局各向异性

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T12 连续前缀、`last_completed_task=T12`、`active_task=null`、`next_task=T13` 一致后才激活 T13。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink/HEAD 均为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/basis.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `RBFKernel::{get_global_anisotropy,scaled_radius,_Global_Plunge,_Transform}`，以及 `ACubic/AGaussian/AMQ/ATPS/AIMQ/AR::{构造,析构,basis,dx_p1,dx_p2,dy_p1,dy_p2,dz_p1,dz_p2,dxx,dxy,dxz,dyx,dyy,dyz,dzx,dzy,dzz,clone}`；核对 `surfe_lib/modeling_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::GRBF_Modelling_Methods::{setup_basis_functions,create_rbf_kernel}` 的六个 anisotropic 工厂、三个无对应类的拒绝和嵌套错误路径，并核对 `surfe_lib/grbf_exceptions.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 global/anisotropic 错误。为逐舍入复现 `Matrix3f SelfAdjointEigenSolver`，还读取冻结 gitlink `Eigen/src/Eigenvalues/{SelfAdjointEigenSolver,Tridiagonalization}.h@36b95962756c1fce8e29b1f8bc45967f30773c00` 与 `Eigen/src/Jacobi/Jacobi.h@36b95962756c1fce8e29b1f8bc45967f30773c00` 的 3×3 三对角化、隐式 QR、Givens 和升序排序路径；未实现 T14 Modified Kernel。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/kernel/mod.rs`、`src/kernel/isotropic.rs`、`src/kernel/anisotropy.rs`、`tests/anisotropic_kernels.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t13_anisotropy_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖纯 Rust `AnisotropicKernel` 与 `AnisotropyError`，只承载冻结工厂实际存在的 Cubic/Gaussian/MQ/TPS/IMQ/R 六类。normal 外积按 double 累加后舍入到 f32；纯 Rust 3×3 self-adjoint eigensolver逐步保留冻结 Eigen 的 lower-triangle 缩放、专用三对角化、binary32 隐式 QR/Givens、升序选择排序和 rescale；只对前两个严格 `< 0.0001f` 特征值截断，并按 f32 运算顺序形成支持矩阵。`scaled_radius` 仅使用 x/y/z，值、两点一阶导数和完整 mixed Hessian 保留 f32 transform 乘 double delta、f32 transform 列内积、Hessian 别名与六类源码表达式；AR 导数继续映射既有类型化错误。少于两个 planar、无冻结 anisotropic 类、非有限 f32 计算和特征分解不收敛均有明确安全错误；没有把 MQ3/WendlandC2/MaternC4 臆造为各向异性能力。
- 测试优先证据：先加入 `tests/anisotropic_kernels.rs` 和发布 allowlist，首次 `cargo test --test anisotropic_kernels` 按预期因根模块缺少 `AnisotropicKernel/AnisotropyError` 而编译失败；实现后 9 个 T13 测试在 debug/release 均通过，覆盖 exact f32 eigen/transform/plunge、六类值、五类完整导数、单位协方差极限、退化与 `0.0001f` 两侧、统一 normal 缩放、矩阵对称性质、两层有限差分、`c` 忽略、AR 哨兵、少于两个 planar、三个不支持类和 f32 overflow 安全拒绝。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen、`basis.cpp`、`modelling_input.cpp` 和 `math_methods.cpp` 编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出逐字节相同。probe 保存 identity/oblique/degenerate 的 f32 eigenvalues、transform、plunge，五个完整核的 basis/两点一阶导数/9 项 Hessian、AR value/整数 `-666`、非零 `c` 忽略和不足 planar 异常。probe source、binary、输出 SHA-256 分别为 `e3a2c42735cbd8497b56cd4ce6b3b13e8cb34b7c865eaaa6bcfefea30d52db84`、`2ad9bd19f9ff309b730e14b54ca9acc858aa61b88350d3f5610634ecdc719c59`、`a13b9cb6dec2e52781a76c14fc7f405f5573a6e5359f79041724ebe74116f87a`。
- 验证命令：reference HEAD/clean-status 与 Eigen gitlink/HEAD；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test anisotropic_kernels`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T13 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 68 个测试（9 个 T13、9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T13 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 34 个文件，包含 T13 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（274.7 KiB，压缩后 55.4 KiB）。
- Parity 证据：oblique case 的三个 f32 eigenvalues、9 项 transform 和三项 plunge 与冻结 probe 逐 bit 一致；六类各向异性 value 路径及五类完整导数路径共 81 个 binary64 输出逐 bit 一致，AR 的 `-666` 分支、`c` 忽略和不足 planar 错误也一致。单位/退化/截断分支和 normal 尺度性质通过，支持矩阵的数值对称与 plunge 单位长度通过 T04 f32 阈值，五类值的一阶导数和一阶导数的 mixed Hessian 同时通过中央有限差分，完成 C++ golden、解析性质和数值三角验证。未生成 T32 正式 fixture，也未宣称 Modified Kernel 或全局 parity。
- 性能证据：特征分解固定为常数规模纯 Rust 3×3 路径，核值/导数为常数时间直接求值，并通过 T13 release 测试和 release build；未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：冻结 `_Global_Plunge` 写后无读取，仍只作 evidence；anisotropic `scaled_radius` 忽略 `c`，而普通 radius 包含 `c`；MQ3/WendlandC2/MaternC4 没有 anisotropic 工厂类。以上 T13 范围已关闭；Modified Kernel 消去组合仍严格归既有 T14，统一泛函归 T15。冻结 C++ probe 构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning。没有新增任务；下一任务固定为 T14。

## T14 — Modified Kernel 全组合

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整分段读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T13 连续前缀、`last_completed_task=T13`、`active_task=null`、`next_task=T14` 一致后才激活 T14。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink/HEAD 仍为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/basis.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Modified_Kernel::{Modified_Kernel,basis_pt_pt,basis_pt_planar_x,basis_planar_x_pt,basis_pt_planar_y,basis_planar_y_pt,basis_pt_planar_z,basis_planar_z_pt,basis_pt_tangent,basis_tangent_pt,basis_planar_planar,basis_tangent_tangent,basis_planar_tangent,basis_tangent_planar,clone}`、复制/析构所有权及其对 `Lagrangian_Polynomial_Basis::{poly,poly_dx,poly_dy,poly_dz,unisolvent_subset_points}` 与 `RBFKernel` 全值/导数面的消费；补充核对 `surfe_lib/modeling_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::GRBF_Modelling_Methods::setup_basis_functions` 的 modified 构造和外层异常包装，以及 `surfe_lib/grbf_exceptions.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 modified/Lagrangian 类别。没有实现 T15 泛函或 T17 系统装配。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/kernel/mod.rs`、`src/kernel/modified.rs`、`tests/modified_kernel.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t14_modified_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖 `ModifiedKernel`，安全拥有普通或各向异性底层核与 T11 Lagrangian basis；逐控制流保留 value、分别作用于第一/第二点的梯度、row-by-column 3×3 mixed Hessian 的 `base-t1-t2+t3+t4`、j/k 循环、固定单位对角和浮点乘法次序，并以 `KernelEvaluation` 暴露中间证据。Value/Planar/Tangent 全组合由同一结果按冻结方向收缩；九个普通 value/八个普通完整导数以及六个各向异性 value/五个各向异性完整导数路径均可用，`R/AR` 只保留 value 并延续 `LinearDerivativeUnavailable`。modified 构造失败映射公开外层 `ModifiedKernelCreationFailure`，独立 T11 构造类别不变；Rust 所有权消除冻结浅拷贝 Lagrangian 与不释放造成的泄漏，不改变有效数值。
- 测试优先证据：先加入 `tests/modified_kernel.rs` 和发布 allowlist，首次 `cargo test --test modified_kernel` 按预期因根模块缺少 `ModifiedKernel` 而编译失败；实现后 8 个 T14 测试在 debug/release 均通过，覆盖所有 13 个冻结组合入口、value/gradient/Hessian 中间对应、普通/各向异性、全部支持核、零距离、Gaussian 固定对角消去、参数交换/Hessian 转置、两层中央有限差分、无效 Lagrangian 构造和 linear 类型化错误。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen、`basis.cpp`、`modelling_input.cpp` 和 `math_methods.cpp` 编译被忽略探针；首次命令把 Eigen include 写成不存在的 `externals/eigen-git-mirror` 而失败，改用冻结实际根目录 `eigen-git-mirror` 后成功。固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出逐字节相同；probe 覆盖 Cubic 普通/各向异性 separated 与 zero-distance 的全部 value/gradient/Hessian、九项 Tangent 组合、Gaussian 四点双向消去、linear value/整数 `-666` 和 invalid Lagrangian 异常。最终 probe source、binary、输出 SHA-256 分别为 `7ab2a14aabbbc12e612b88dbf74b35a08ef676fcff355229f9a63a22309b54c1`、`00ef598bc794d06426502005f39776ad8cfc640a2d620a4f1570db6f4a6489ab`、`1f676e7ce88698639b5dbf163986d43d847011c378e0b90d825f5668f6e389a0`。
- 验证命令：reference HEAD/clean-status 与 Eigen gitlink/HEAD；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test modified_kernel`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T14 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 76 个测试（8 个 T14、9 个 T13、9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T14 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 36 个文件，包含 T14 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（309.4 KiB，压缩后 60.7 KiB）。
- Parity 证据：普通/各向异性 separated 的两组 16 项 value/两点梯度/Hessian、两组九项 Tangent 组合和两组 16 项 zero-distance 共 82 个 binary64 输出逐位匹配冻结 probe；Gaussian 四点双向消去的八项残量与 linear value 也逐位匹配，`-666` 和 invalid 构造分支一致。首次差分捕获 Hessian `t4` 交换因子导致的 1–2 ULP 偏差，按冻结 `p2(k)*K(Uj,Uk)*p1(j)` 次序修正后逐位通过；交换/转置、固定对角消去和两层有限差分完成三角验证。未生成 T32 正式 fixture，也未宣称系统或全局 parity。
- 性能证据：T14 保持冻结四点/十二非对角项的常数规模稠密直接求值，通过 release build 与任务专属 release 测试；未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：冻结 modified 公式将 Gram 对角固定为 1，仅在 `K(Uj,Uj)=1` 时严格消去；该语义已在兼容文档和 Gaussian 测试中固定，没有“修正”其他核。统一 Value/Derivative/Tangent/Difference 表示仍严格归既有 T15，完整矩阵装配归 T17。冻结构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning。没有新增任务；下一任务固定为 T15。

## T15 — 统一内部线性泛函

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整分段读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T14 连续前缀、`last_completed_task=T14`、`active_task=null`、`next_task=T15` 一致后才激活 T15。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink/HEAD 仍为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/basis.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Kernel::{set_points,set_center,set_evaluation_points,p1,p2,basis_pt_pt,basis_pt_planar_x,basis_planar_x_pt,basis_pt_planar_y,basis_planar_y_pt,basis_pt_planar_z,basis_planar_z_pt,basis_pt_tangent,basis_tangent_pt,basis_planar_planar,basis_tangent_tangent,basis_planar_tangent,basis_tangent_planar}`、`RBFKernel` 全部 13 个组合体和 `Modified_Kernel` 对应全部组合体；逐块核对 `surfe_lib/single_surface.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Single_Surface::get_interpolation_matrix`、`surfe_lib/lajaunie.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Lajaunie_Approach::{_get_increment_pairs,get_interpolation_matrix}`、`surfe_lib/stratigraphic_surfaces.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Stratigraphic_Surfaces::{_get_increment_pairs,get_interpolation_matrix}`、`surfe_lib/continuous_property.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Continuous_Property::get_interpolation_matrix` 和 `surfe_lib/vector_field.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Vector_Field::get_interpolation_matrix` 的 Value/Planar 分量/Tangent 与 `(v1-v2)-(v3-v4)` increment 符号、行列作用点。没有实现 T16 layout 或 T17 数值装配。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/functional.rs`、`tests/functionals.rs`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t15_functional_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖 `LinearFunctional::{Value,Derivative,Tangent,Difference}`，每项安全拥有作用点，Derivative 明确 Cartesian 轴，Tangent 保留原始方向，Difference 明确 positive/minus-negative 端点；新增稳定 `DofLabel`、可审查的带系数 primitive expansion，以及借用普通 isotropic、普通 anisotropic 或 Modified Kernel 的 `FunctionalKernel` 统一双线性调用层。原始 25 种 Value/Derivative/Tangent 组合逐项保持第一/第二参数导数方向、row-by-column Hessian 与 C++ 乘加顺序；Difference 对任意原始泛函递归线性展开，双 Difference 特别保持冻结 `(v1-v2)-(v3-v4)` 括号和舍入顺序。Linear/AR derivative 错误沿组合直接传播；没有约束重排、模型索引或矩阵布局。
- 测试优先证据：先加入 `tests/functionals.rs` 和发布 allowlist，首次 `cargo test --test functionals` 按预期因根模块缺少 `DofLabel/FunctionalKernel/FunctionalPrimitive/LinearFunctional` 而编译失败；实现后 7 个 T15 测试在 debug/release 均通过，覆盖四种统一核路径的全部 25 个原始组合和 11 个 Difference 组合、标签与展开系数、冻结差值括号、差值/方向线性、参数交换、径向符号、两个核参数上的 derivative/tangent 中央有限差分和 Linear 类型化错误传播。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen、`basis.cpp`、`modelling_input.cpp` 和 `math_methods.cpp` 编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次生成 8 行、152 个 token 并逐字节相同。探针覆盖 Cubic isotropic、ACubic anisotropic、两者各自 Modified Kernel 的 25 个直接 `basis_*` 组合及 11 个 Lajaunie/Stratigraphic 风格 Difference 组合。最终 probe source、binary、输出 SHA-256 分别为 `7da7b4163277d151af9af2814f3fdf4f88856ec352b1a3979568060b51ec25f5`、`ad812ff931f59fe5b262b5095fa80118c7da46cd5ace2384a4a9dc7597ca50c0`、`e2793e9932523bed136eadd058dca763e31f039640ca20b91b1c93e023b27d5a`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test functionals`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T15 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 83 个测试（7 个 T15、8 个 T14、9 个 T13、9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T15 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 38 个文件，包含 T15 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（343.4 KiB，压缩后 66.8 KiB）。
- Parity 证据：四种核路径的 100 个原始泛函输出与 44 个 Difference 输出共 144 个 binary64 结果逐位匹配冻结 hexfloat；Difference 双边展开保持源码括号，Value/Derivative/Tangent 的参数交换、径向符号和线性性质通过，两个参数的 derivative/tangent 均通过中央有限差分，完成 C++ golden、解析性质与数值三角验证。未生成 T32 正式 fixture，也未宣称 layout、矩阵或全局 parity。
- 性能证据：普通泛函仍为固定次数直接核求值，Difference 最多展开为四个原始双线性求值，Tangent/Hessian 收缩保持固定 3×3 常数规模，并通过 release build 与 T15 release 测试；未运行 T33 同机模型 benchmark，未作全局性能判断。
- 后续发现：五模型对 Value/Derivative/Tangent 的自由度顺序和分区仍严格归既有 T16，完整 Difference/核/polynomial/RHS/smoothing 数值装配仍归既有 T17。冻结探针构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning。没有新增任务；下一任务固定为 T16。

## T16 — 五种模型的确定性 constraint layout

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整分段读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T15 连续前缀、`last_completed_task=T15`、`active_task=null`、`next_task=T16` 一致后才激活 T16。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/{single_surface,lajaunie,stratigraphic_surfaces,continuous_property,vector_field}.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的构造、`process_input_data`、`get_method_parameters`、`get_interpolation_matrix`、`get_equality_values`、适用的 `get_inequality_matrix/get_inequality_values`、increment/polynomial block 与 solver 尺寸消费点；完整核对 `surfe_lib/modeling_methods.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `InternalParameters` 所有者、`get_interface_data` 的降序 exact-level/reference/singleton 规则、`check_input_data` 和 equality 首行偏移，以及 `surfe_lib/modelling_parameters.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4::InternalParameters`。没有填充任何 T17 核、polynomial、RHS 或 smoothing 数值。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/layout.rs`、`src/model/mod.rs`、`src/model/{single_surface,lajaunie,stratigraphic,continuous_property,vector_field}/mod.rs`、五个对应 `layout.rs`、`tests/layouts.rs`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t16_layout_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖 `ConstraintLayout`，以同一有序 `LayoutDof` 同时描述矩阵行列；提供半开 `IndexRange`、named sections、Equality/Inequality/Bounded/Polynomial 分区、原始与冻结 internal counts、稳定 `dof/role/section/index_of` 查询和逐行 `debug_snapshot`。Single 精确保留 inequality→interface→每 planar 的 x/y/z→tangent→完整 polynomial；Lajaunie 保留各 exact level 首点减其余点→planar→tangent→truncated polynomial；Stratigraphic 保留相邻降序 reference→逐 inequality 的最近上层后最近下层→同层 pair→planar→tangent，并区分普通 QP 的前缀 inequality 与后缀 equality；Continuous 只消费 interface，Vector 只消费 planar 三分量。Restricted 三模型全基块标为 bounded 且无 polynomial；空类别、singleton、exact-level 冲突和不安全负 polynomial 计数映射既有错误，不使用 hash 顺序。输入清洗仍复用 T08，不在布局层隐式重排。
- 测试优先证据：先加入 `tests/layouts.rs` 和发布 allowlist，首次 `cargo test --test layouts` 按预期因根模块缺少 `constraint_layout/ConstraintLayout/LayoutDof` 等 T16 API 而编译失败；实现后 6 个 T16 测试在 debug/release 均通过，覆盖五模型 internal/matrix 维数、全部 section/partition、pair 端点与行列标签、0/1/2 阶和冻结声明的高阶 polynomial 计数、三个 restricted 分支、空类别/singleton/非法层位、去重后反向排列一致性、稳定索引查询和逐字节精确 debug snapshot。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部所需非可视化核心编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出逐字节相同。探针直接运行五模型 `process_input_data/get_method_parameters/get_interpolation_matrix`，记录 internal 参数与矩阵尺寸 `Single=14`、`Lajaunie=12`、`Stratigraphic=14`、`Continuous=5`、`Vector=6`；TraceKernel 记录 Lajaunie pair `[0,1],[3,4]` 与 Stratigraphic 七 pair `[0,2],[2,3],[0,20],[20,2],[3,21],[0,1],[3,4]`，另实测 Stratigraphic restricted 的 `n_inequality=2/n_equality=0`。最终 probe source、binary、输出 SHA-256 分别为 `8e791954b9398e49bc83db1700a53451e25790b80a357070ae8ccf8321703caa`、`e5a1b5551415398c4891333c353ab25281ac4f41c0379343380a2005c2b6c95d`、`da081856e4bf6c00fb958eeffe24dd7a8a125047fcc176752178cc571de2c172`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test layouts`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T16 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 89 个测试（6 个 T16、7 个 T15、8 个 T14、9 个 T13、9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T16 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 51 个文件，包含 T16 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（393.6 KiB，压缩后 75.3 KiB）。
- Parity 证据：五模型全部 internal count/flag 与冻结 probe 精确一致；五个矩阵维数、Lajaunie 两 pair、Stratigraphic 七 pair、普通 equality/inequality 前缀和 restricted bounded 分支均 exact；Rust tests 对每个 section range、代表行列标签、polynomial suffix、空 section 和 input permutation 快照作离散精确比较。没有生成 T32 正式 fixture，也未宣称矩阵数值或全局 parity。
- 性能证据：布局构造为确定性 Vec 追加；除 Stratigraphic 对每个 inequality 线性扫描 distinct levels 外均为约束数线性工作，已通过 release 专测与 release build。未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：Continuous Property 冻结 internal layout 忽略实际容器中的 inequality/planar/tangent，Vector Field 忽略 interface/inequality/tangent；本任务以 source/internal 双计数显式保留而未臆增能力。冻结 polynomial order 超出 0/1/2 时 `get_method_parameters` 仍先计算 tetrahedral 声明维数，后续 basis/matrix 失败留归既有 T17/T30；布局记录声明维数但不宣称装配可用。完整数值装配仍严格归既有 T17；冻结构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning。没有新增任务；下一任务固定为 T17。

## T17 — 统一矩阵、RHS 与 smoothing 装配

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整分段读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T16 连续前缀、`last_completed_task=T16`、`active_task=null`、`next_task=T17` 一致后才激活 T17。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink/HEAD 仍为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：完整逐块核对 `surfe_lib/single_surface.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `_get_polynomial_matrix_block`、`_insert_polynomial_matrix_blocks_in_interpolation_matrix`、`get_interpolation_matrix`、`get_equality_values`、`get_inequality_matrix`、两个 `get_inequality_values` 和 regression smoothing；`surfe_lib/lajaunie.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的对应 polynomial/matrix/equality/bounded/smoothing 路径；`surfe_lib/stratigraphic_surfaces.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 polynomial helpers、完整 increment matrix、equality/inequality matrix/values 和 bounded values；`surfe_lib/continuous_property.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 与 `surfe_lib/vector_field.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的实际可达 matrix/RHS；`surfe_lib/modeling_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::GRBF_Modelling_Methods::get_equality_matrix`；并核对五个对应头文件、`setup_system_solver` 的尺寸/分区消费、`process_input_data` 的 restricted bounds 和 `modelling_input.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::get_largest_distance_between_points`。没有实现 T18 LU 或 T19/T20 QP。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/assembly.rs`、`src/model/{single_surface,lajaunie,stratigraphic,continuous_property,vector_field}/mod.rs`、五个对应 `assembly.rs`、`tests/assembly.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t17_assembly_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖 row-major `DenseMatrix/DenseVector`、稳定 binary64 matrix snapshot、`AssembledSystem` 和 Linear/ordinary-QP/Bounded 三种 solver-facing 输入，但不调用求解器。约束块统一由 T15 泛函按 T16 行列标签直接装配，Difference 保持冻结括号；Single/Lajaunie 的完整或 truncated polynomial `P/PT/0` 逐项保持项序、导数、tangent 乘加和转置；五模型 RHS、Single level 符号行、Stratigraphic equality/inequality 前后缀、三模型 restricted `b/r` 及最大距离语义均按源码保留。Regression smoothing 精确保留“替换而非相加”：Single 只替换 inequality+interface 对角，Lajaunie 只替换 same-level increment 对角，值为 `K((0,0,0),(0,0,amount))`；amount=0 仍执行。layout/kernel modified 状态不符、unsupported polynomial 和 `R/AR` derivative 均安全类型化失败；没有因条件数拒绝装配。
- 测试优先证据：先加入 `tests/assembly.rs` 和发布 allowlist，首次 `cargo test --test assembly` 按预期因根模块缺少 `assemble_system/AssemblyConstraints/AssemblyError/ConstraintSystem/DenseMatrix/DenseVector` 而编译失败；实现后 6 个 T17 测试在 debug/release 均通过，覆盖五模型、普通/Modified、普通/各向异性、完整/截断 polynomial、ordinary QP 行切片与符号、restricted lower/range、smoothing 正值/零值、全矩阵 snapshot、对称性、维数、错误阶段和 kernel/layout 不变量。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部所需非可视化核心编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次生成 14 行、53 个 token 并逐字节相同。probe 直接运行五模型 matrix/RHS、Single/Stratigraphic ordinary QP、Single/Lajaunie/Stratigraphic bounded、Single/Lajaunie smoothing、Vector ordinary anisotropic 和 Single Modified anisotropic，并对每个完整 row-major binary64 matrix/vector 加维数感知位哈希。最终 probe source、binary、输出 SHA-256 分别为 `5947b63414043e42845f44b2c1466b4eb45fd1e4c48b81ccc5862903d329c5a3`、`7be9a341fc547958abf5cd005903889c439803564b5dc73f1dc23c1cba0a21f9`、`6b8d77285e6e2bcbef04a23025db12506253464da0183aad6297d1e77ea7849f`。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test assembly`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T17 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 95 个测试（6 个 T17、6 个 T16、7 个 T15、8 个 T14、9 个 T13、9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T17 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 58 个文件，包含 T17 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（450.8 KiB，压缩后 84.7 KiB）。
- Parity 证据：冻结 probe 的 13 组完整 matrix、equality/inequality/RHS/lower/range/smoothing 输出全部由 Rust 测试以相同维数感知 row-major binary64 哈希逐位匹配；其中包含 10×10 Single/Lajaunie polynomial 系统、12×12 两个 ordinary QP、三组 bounded、6×6/15×15 Vector ordinary/anisotropic 和 24×24 Single Modified anisotropic。测试另精确检查 polynomial 项、QP 符号/分区、smoothing 覆盖范围与边界，并以 T04 阈值检查全矩阵数值对称性。未生成 T32 正式 fixture，也未宣称求解或全局 parity。
- 性能证据：装配保持冻结稠密 `O(n²)` 直接核求值和 row-major 连续存储，通过 release build 与 T17 release 专测；未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：冻结 Continuous Property 的私有 polynomial helpers 在实际 `poly_term=false/n_poly_terms=0` 路径不可达且含 `.size()` 维数问题，已在兼容文档记录，仍由既有 T27 做最终可达性复核；两 normal 的 rank-two anisotropy 探索数据在 Eigen/Rust `f32` eigensolver 低位存在平台舍入差异，T17 正式 oblique anisotropy 数据按 T04 规则精确通过，覆盖扩展仍归既有 T32。LU、ordinary QP 与 LOQO 数值求解分别严格归既有 T18/T19/T20。冻结构建再次出现已登记到 T31 的 Tangent residual selector 漏返回 warning。没有新增任务；下一任务固定为 T18。

## T18 — Pure-Rust partial-pivot LU

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整分段读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T17 连续前缀、`last_completed_task=T17`、`active_task=null`、`next_task=T18` 一致后才激活 T18。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink/HEAD 仍为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/matrix_solver.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `System_Solver`、`Linear_LU_decomposition::{Linear_LU_decomposition,solve,validate_matrix_systems,check_solution}`，并核对五模型 `setup_system_solver` 的 `solve()==false` 到 `linearsolverfailure` 消费点；为保留实际 Eigen 路径，完整读取冻结 gitlink `Eigen/src/LU/PartialPivLU.h@36b95962756c1fce8e29b1f8bc45967f30773c00` 的 `PartialPivLU::{compute,_solve_impl}`、`internal::partial_lu_impl::{unblocked_lu,blocked_lu}`，以及 `Eigen/src/Core/{SolveTriangular.h,products/TriangularSolverVector.h}@36b95962756c1fce8e29b1f8bc45967f30773c00` 的 dynamic column-major vector 前后代入。没有实现 T19 ordinary QP 或 T20 LOQO。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/solver/{mod,error,lu}.rs`、`tests/lu_solver.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t18_lu_probe.cpp`、binary 和两份输出只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖纯 Rust row-major partial-pivot LU，以首个最大绝对值作每列主元，保留整行交换、`a -= l*u` 尾块更新和冻结 dynamic column-major UnitLower/Upper RHS 更新次序；公开 row transpositions、Eigen permutation indices、pivot values、首个 exact-zero pivot 和 packed LU。矩阵 storage/空/方阵/RHS 维数、matrix/RHS 有限性分别形成 `LuValidation`；非有限输入仍尝试求解并保存 evidence，非法形状在 Rust 边界安全拒绝。成功后计算 L2/relative-L2/L-infinity residual、constraint feasibility 等价的 scale-aware backward error 和 `64*EPSILON*n` 后验门槛，不使用 condition number。失败保留 attempted、factorization、适用的 candidate weights/residual，并稳定映射外层 `Error::LinearSolverFailure`。
- 测试优先证据：先加入 `tests/lu_solver.rs` 和发布 allowlist，首次 `cargo test --test lu_solver` 按预期因根模块缺少 `solve_partial_pivot_lu/validate_lu_system/LuSolveErrorKind` 而编译失败；实现后 8 个 T18 测试在 debug/release 均通过，覆盖良态、3×3 Hilbert、绝对值 pivot tie、奇异一致/不一致、non-finite matrix/RHS、storage/空/非方阵/维数错误，以及 1–12 阶确定性对角占优已知解和 backward-error。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen、`matrix_solver.cpp` 和 `math_methods.cpp` 编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次输出逐字节相同。probe 直接运行 `validate_matrix_systems/solve` 并用同一冻结 Eigen 暴露 transpositions/permutation/packed LU，覆盖 T04 固定 well/Hilbert/singular/non-finite family、pivot tie、奇异不一致、non-finite RHS 和维数不符。最终 probe source、binary、输出 SHA-256 分别为 `eac3ba6f1dc33bac8a70e3af19f4e3832aac9e175e6615f3eedb26f3d3a655cf`、`de130009b5e1b9d51d0316a655783e4f5ae95428629ff00e906a7daf1fba3446`、`05ffb95e6d116981d65adbb208b92e847fd2b041506c6e4c16e3eddf2ea35452`。
- 验证命令：reference HEAD/clean-status 与 Eigen gitlink/HEAD；上述 C++ 构建和两次确定性运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test lu_solver`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T18 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 103 个测试（8 个 T18、6 个 T17、6 个 T16、7 个 T15、8 个 T14、9 个 T13、9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T18 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 62 个文件，包含 T18 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（479.5 KiB，压缩后 91.1 KiB）。
- Parity 证据：well 和 Hilbert 的 weights、row transpositions、Eigen permutation indices 与完整 packed LU 均逐 binary64 bit 匹配冻结 probe；pivot tie 精确选择首个绝对值最大行并得到相同 `[1,2,2]` transpositions。冻结 Eigen 在 Upper RHS 分量精确为零时跳过除法，故 singular-consistent `[[1,2],[2,4]]/[3,6]` 实测成功、weights `[3,0]`、residual 零；Rust 精确保留，而相同 matrix 的不一致 RHS 在尝试后以 `SingularSystem` 失败。non-finite matrix/RHS 的验证与 attempted/success 离散行为、维数失败和外层类别一致；Hilbert residual 与冻结值满足 T04 `solver_residual` 层，确定性已知解全部通过 scale-aware backward-error。未生成 T32 正式 fixture，也未宣称 QP 或全局 parity。
- 性能证据：分解保持稠密 `O(n³)`、额外 packed matrix/pivot `O(n²)` 的直接纯 Rust 路径，前后代入 `O(n²)`；通过 release build 与 T18 release 专测。未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：冻结 `validate_matrix_systems` 只检查 matrix finite 且模型路径不调用，`check_solution` 只打印 relative residual 并恒定返回 true；debug condition number 不参与 release 接受/拒绝。奇异一致系统的有限非唯一成功是冻结可观察行为，不得按“有 zero pivot”预拒绝。以上已在 T18 兼容文档和测试关闭；ordinary predictor-corrector QP 仍严格归既有 T19。没有新增任务；下一任务固定为 T19。

## T19 — 普通 predictor-corrector QP

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整分段读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T18 连续前缀、`last_completed_task=T18`、`active_task=null`、`next_task=T19` 一致后才激活 T19。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink/HEAD 仍为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/matrix_solver.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `System_Solver`、`Quadratic_Predictor_Corrector::{Quadratic_Predictor_Corrector,solve,validate_matrix_systems}` 及 interpolation→Hessian `2.0*matrix`、注释掉验证后直接调用 QP 和 weights 消费；完整读取 `math_lib/math_methods.h@290dbe0ab344f4258a4935f05cad0f153f0f69a4`，并逐控制流核对 `math_lib/math_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Math_methods::{quadratic_solver,_find_step_length,max_element_wrt_zero}` 的初始化、四块 KKT、首次 affine shift、predictor/corrector、步长、`mu`、停止和方向提取。另核对 `_find_step/_find_positivity_step` 只由 LOQO 消费，保持为既有 T20 边界；普通 KKT 继续复用 T18 已按冻结 Eigen `PartialPivLU` 证明的纯 Rust 路径，没有实现 restricted-range/LOQO。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/assembly.rs`、`src/solver/mod.rs`、`src/solver/qp_predictor_corrector.rs`、`tests/qp_predictor_corrector.rs`、`docs/port/compatibility.md`、`docs/port/STATE.json`、`docs/port/JOURNAL.md`。被忽略的 `.cache/surfe-oracle/t19_qp_probe.cpp` 和 binary 只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖纯 Rust ordinary primal-dual predictor-corrector QP；严格保留 `H=2*interpolation`、零初值、`sqrt(H.maxCoeff())` complementary 初值、首次完整 affine step、`1000+2*max_violation` shift、逐轮 KKT residual、predictor、`_find_step_length` 的符号/严格 `1e-14`/unit-cap、`mu_aff`、立方 `sigma`、corrector、方向提取和更新次序。停止顺序精确保留第六轮后 `mu` 回升优先、再 `mu<1e-8`；新增默认 10000 轮安全上限但不会放宽源码规则。每轮暴露 `mu`、objective、stationarity、equality/inequality residual、实际 min slack、affine/corrector step、`mu_aff/sigma`，每次 KKT 暴露 T18 pivot/residual evidence；完整 terminal evidence 使用 T04 residual/feasibility 层。冻结 LLT validator 单独保存为 evidence 而不作 solve gate，不用 condition number；公开安全 row-major/vector/constraint constructors 让 T17 输出和显式 QP 用例共享同一表示。
- 源缺陷兼容：冻结 `nc=0` 会除零并索引空 vector，Rust 安全分类 `MissingInequalities`。冻结不可行 `x=0,x>=1` 在 `mu` 变负后错误返回成功，另一个 indefinite 用例在第六轮 `mu` 回升后返回违反约束的成功 candidate；两者先由 oracle 固定完整停止 trace 和 weights，Rust 保留同一停止/candidate 后以 `InfeasibleSolution` 和外层 `Error::PredictorCorrectorSolverFailure` 拒绝。zero-Hessian 可行退化样例的有限非唯一 `[1,1000]`、零 objective/terminal `mu` 则逐 bit 保留并以后验 residual/feasibility 接受。
- 测试优先证据：先加入 `tests/qp_predictor_corrector.rs` 和发布 allowlist，首次 `cargo test --test qp_predictor_corrector` 按预期因根模块缺少 QP API/类型以及安全 dense constructors 而编译失败；实现后 10 个 T19 测试在 debug/release 均通过，覆盖 inactive/active boundary、无 equality、多 inequality、step helper、LLT 非 gate、病态、indefinite、zero-Hessian 退化、冻结两种错误成功的不可行分支、所有 shape/finite/缺 inequality 错误与显式 iteration limit。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen、`matrix_solver.cpp` 和 `math_methods.cpp` 编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 对 inactive、active、no-equality、mixed、infeasible、ill-conditioned、indefinite、zero-Hessian 和 sixth-iteration-mu-increase 九类分别直接运行 `Quadratic_Predictor_Corrector::{validate_matrix_systems,solve}`，利用源码无条件 `mu[iter]` 输出保存逐轮 trace，并记录 weights/objective/equality/min-slack。两次完整 suite 逐字节相同。最终 probe source、binary、suite 输出 SHA-256 分别为 `19603cdcf377300270578cd4430d9b6dfe31eb2b7fd7fb1a11bd5d7481f83089`、`97a2c1d26a02ec1220618d24fc98adc7242520cc3e6ad1b865477b2d3f0e8c82`、`4bca64037b39cabb8f128ea12fdbc3b8b32850bbae320ff15e8b6b182efda629`。
- 验证命令：reference HEAD/clean-status 与 Eigen gitlink/HEAD；上述 C++ 构建、九类两次固定环境运行/`cmp`/SHA-256；`cargo fmt --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test qp_predictor_corrector`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T19 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 113 个测试（10 个 T19、8 个 T18、6 个 T17、6 个 T16、7 个 T15、8 个 T14、9 个 T13、9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T19 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 64 个文件，包含 T19 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（528.8 KiB，压缩后 100.5 KiB）。
- Parity 证据：九类冻结 cases 的 terminal weights/candidate 全部逐 binary64 bit 匹配；inactive/active/no-equality/mixed/ill-conditioned/indefinite/zero-Hessian 的 objective、equality、min-slack 与停止分支一致，逐轮 `mu` 的 iteration index exact 且数值满足 T04 `solver_residual` 层。病态系统实际尝试且成功；indefinite/zero-Hessian 的 frozen LLT 失败没有改变 solve 分支。不可行两例的 frozen success 缺陷没有伪装为有效 parity，而是以 exact source trace/candidate 加 Rust 后验失败形成可审计兼容证据。未生成 T32 正式 fixture，也未宣称 LOQO 或全局 parity。
- 性能证据：每次 predictor/corrector 复用纯 Rust稠密 partial-pivot LU，保持冻结 `O(k³)`、`k=n+na+2nc`，其余矩阵/残差工作为 `O(k²)`；T19 release 专测和 release build 通过。未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：LOQO 的 `_find_step/_find_positivity_step`、上下界变量和 restricted-range 仍严格归既有 T20；T19 未实现或提前测试 LOQO。没有新增任务；下一任务固定为 T20。

## T20 — Restricted-range / LOQO 风格 QP

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整分段读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T19 连续前缀、`last_completed_task=T19`、`active_task=null`、`next_task=T20` 一致后才激活 T20。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净，Eigen gitlink/HEAD 仍为 `36b95962756c1fce8e29b1f8bc45967f30773c00`。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/matrix_solver.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `Quadratic_Predictor_Corrector_LOQO::{Quadratic_Predictor_Corrector_LOQO,solve,validate_matrix_systems}`；完整读取 `math_lib/math_methods.h`，并逐控制流核对 `math_lib/math_methods.cpp@290dbe0ab344f4258a4935f05cad0f153f0f69a4::Math_methods::{quadratic_solver_loqo,_find_step,_find_positivity_step}` 的初始化、两种 KKT、D/E 消元、十组 primal-dual/slack 变量、predictor/corrector、中心参数、步长、停止和失败；另核对 `surfe_lib/surfe_api.{h,cpp}::SetRestrictedRange` 以及 Single/Lajaunie/Stratigraphic 的 `get_inequality_values/setup_system_solver` bounded 消费路径。没有执行 T21 的活跃约束重建或二次 LU。
- 修改文件：`Cargo.toml`、`src/lib.rs`、`src/solver/{mod,lu,qp_loqo}.rs`、`tests/qp_loqo.rs`、`docs/port/{compatibility.md,STATE.json,JOURNAL.md}`。T18 LU 仅补充保留已实际计算的 non-finite candidate evidence，使 LOQO 能精确保留冻结初始 solve 后再于 predictor 拒绝的阶段；T18 的成功/失败类别、数值和后验门槛未改变。被忽略的 `.cache/surfe-oracle/t20_loqo_probe.cpp`、binary、两份输出和诊断探针只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖纯 Rust restricted-range primal-dual LOQO，严格保留 `H=2*interpolation`、`b<=A*x<=b+r`、初始 `[-(H+I),A';A,I]` KKT、`100` 级 primal/dual/slack 初值、D/E 对角消元、`P*Q^-1` 与 `P^-1*Q` 的源方向、predictor/corrector T18 LU、四组 positivity divisor、`0.95` fraction-to-boundary、平方中心 fraction 和双步长更新。冻结唯一成功 `significant_figures>6`、dual-above-primal 和 significant-figures-decrease 失败顺序不变；默认 10000 轮只作安全上限。公开 shape/finite/LLT evidence、完整逐轮目标/残差/步长/`mu`、逐阶段 KKT pivot/residual、终点上下界 slack/互补性/接受阈值和稳定外层 `Error::LoqoSolverFailure`；成功另须通过有限性、残差、闭区间可行性与互补性后验检查，不使用 condition number。
- 源缺陷兼容：冻结 LLT validator 不被 `solve()` 调用，故 indefinite、zero-Hessian 和病态输入仍先尝试；Rust 同样不作正定 gate。冻结初始 solve 不检查 weights 有限性，Rust 保留 candidate 和第一次 iteration evidence 后在 predictor 安全失败，不复制后续非有限运算。源码第一次打印读取未初始化 residual vectors，只影响诊断；Rust 使用确定性零 evidence，匹配冻结 probe 的观察值而不复制 UB。zero-Hessian 的有限负极小 candidate 及 negative-zero objective 逐 bit 保留；tight zero-width、negative range 与 indefinite 的源失败分支保留，不把数学上可行的 tight 用例改成另一算法的成功。
- 测试优先证据：先加入 `tests/qp_loqo.rs` 和发布 allowlist，首次 `cargo test --test qp_loqo` 按预期因根模块缺少 LOQO API/类型而编译失败；实现后 9 个 T20 测试在 debug/release 均通过，覆盖 inactive、lower/upper active、两变量双边 box、`0.95` helper、完整 Initial/Predictor/Corrector stage 顺序、LLT 非 gate、`1e-14` 病态、zero-Hessian、tight、indefinite、negative-range、non-finite、shape 错误和显式 iteration limit。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen、`matrix_solver.cpp` 和 `math_methods.cpp` 编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 对 inactive、active、two-variable、tight、ill-conditioned、zero-Hessian、indefinite、negative-range 和 non-finite 九类运行 `validate_matrix_systems/solve` 并保留源码逐轮输出。两次完整 suite 逐字节相同；最终 probe source、binary、输出 SHA-256 分别为 `99c8a706a95ca6e7ac72e2c69500c2bb4fdd258354a74d10b741e1eb07357dfb`、`77450704fcc4c520bdb98af9b20bcf1682760b235c133f323d4ef1418efd21ff`、`d876c49ef3716533080f250c15e7bdf77d707fdff2fbe6f4aff1b6a95abc6cff`。
- 验证命令：reference HEAD/clean-status 与 Eigen gitlink/HEAD；上述 C++ 构建、九类两次固定环境运行/`cmp`/SHA-256；`cargo fmt --all -- --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test qp_loqo`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T20 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 122 个测试（9 个 T20、10 个 T19、8 个 T18、6 个 T17、6 个 T16、7 个 T15、8 个 T14、9 个 T13、9 个 T12、6 个 T11、4 个 T10、7 个 T09、5 个 T08、9 个 T07、8 个 T06、11 个协议/fixture）且全通过，T20 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 66 个文件，包含 T20 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（584.8 KiB，压缩后 110.2 KiB）。
- Parity 证据：inactive、active、two-variable、ill-conditioned 和 zero-Hessian 的 terminal weights/objectives 逐 binary64 bit 匹配冻结 probe；停止 iteration、关键目标、`mu`、KKT stage 数及 tight/indefinite/negative-range/non-finite 离散失败分支一致，完整数值 trace 满足 T04 `solver_residual` 层。解析 lower/upper-active box 解、双变量最优解、闭区间 slack、stationarity、互补性和 objective 同时通过，形成 C++ golden、解析最优解和后验 KKT/可行性三角验证。未生成 T32 正式 fixture，也未宣称重建、模型端到端或全局 parity。
- 性能证据：每轮复用纯 Rust 稠密 partial-pivot LU，保持冻结 `O(n³)` KKT 主成本与 `O(n²)` 矩阵/残差工作；T20 release 专测和 release build 通过。未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：冻结 tight zero-width box 在第一轮因 dual objective 超过 primal objective 返回失败，已按可观察行为关闭；初始 residual 打印的未初始化读取仅作上述安全诊断差异。QP 活跃结果转普通 RBF 约束、重装配和再次 LU 仍严格归既有 T21。没有新增任务；下一任务固定为 T21。

## T21 — QP 到普通 RBF 线性系统的重建

- 日期：2026-08-13。
- 状态：完成。恢复上下文时根目录没有 `AGENTS.md`；任务开始前 `main` 分支工作区干净。完整分段读取 `STATE.json`、`PLAN.md` 和 `JOURNAL.md`，校验 INIT–T20 连续前缀、`last_completed_task=T20`、`active_task=null`、`next_task=T21` 一致后才激活 T21。
- Surfe reference：按优先级解析到被忽略的 `.cache/surfe-reference`；`git rev-parse HEAD` 精确为 `290dbe0ab344f4258a4935f05cad0f153f0f69a4`，reference 工作树干净。
- 阅读的 Surfe 源码：完整核对 `surfe_lib/single_surface.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4`、`surfe_lib/lajaunie.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 和 `surfe_lib/stratigraphic_surfaces.{h,cpp}@290dbe0ab344f4258a4935f05cad0f153f0f69a4` 的 `convert_modified_kernel_to_rbf_kernel`、solver setup、inequality/equality values、input processing、matrix rebuild、LU 和 iso-value 更新；补充核对 `modelling_input` 约束 setter 与 `basis` polynomial 消费点。T02 已证明三个 conversion body 无冻结公开根调用边；本任务不伪造可达性，而把它们作为显式操作移植，也没有提前实现 T22 模型 API。
- 修改文件：`Cargo.toml`、`src/assembly.rs`、`src/constraints.rs`、`src/lib.rs`、`src/model/{mod,reconstruct}.rs`、`tests/reconstruct.rs`、`docs/port/{compatibility.md,STATE.json,JOURNAL.md}`。被忽略的 `.cache/surfe-oracle/t21_reconstruct_probe.cpp` 和 `.cache/surfe-oracle/t21-reconstruct-probe` 只作冻结 C++ oracle，不进入生产、正常测试或发布包。
- 核心实现：新增零依赖纯 Rust `solve_and_reconstruct`，按 source layout 先执行 T19 ordinary predictor-corrector 或 T20 bounded LOQO，再进入共同重建；另提供消费已经求得 QP weights 的 `reconstruct_from_qp_weights`，作为隔离 QP 收敛与验证 conversion body 的确定性 seam。重建不会复用 QP weights 跳过源码步骤：先用 Modified Kernel 在每个源自由度求新 scalar/gradient/tangent-inner-product，重建普通 layout，完整二次装配矩阵/RHS，再执行 T18 partial-pivot LU。结果保留源 QP solution、源/目标 dof 索引与目标值映射、重建约束、矩阵/RHS、smoothing、LU residual/pivot evidence 和任意见证点的前后 scalar/gradient。
- 离散转换：冻结源码没有 multiplier/slack active-set filter，因此所有 source QP dofs 均被保留。Single Surface 依次把全部 inequality 转为 interface，再接原 interfaces，然后 planar/tangent；Lajaunie/Stratigraphic 保留全部 increment pairs、planar/tangent 并把最终 partition 全部作为 equality。planar 只替换 stored normal，保留 strike/dip/polarity/bounds；tangent 只替换 inner product。Stratigraphic 强制一阶 truncated 三项 polynomial 并保留冻结对象没有刷新的 stale `n_inequality` evidence；Lajaunie 同样强制 `n_poly_terms=3`，但 basis 仍服从原 polynomial order，故非一阶输入在第二次装配安全映射为 `Reassembly` 失败。阶段错误稳定区分 `SourceAssembly`、`Qp`、`Reassembly` 与 `Lu`。
- 测试优先证据：先加入 `tests/reconstruct.rs` 和发布 allowlist，首次 `cargo test --test reconstruct` 按预期因根模块缺少 reconstruction API 而编译失败；实现后 8 个 T21 测试在 debug/release 均通过。覆盖三模型 arbitrary solved weights、精确 mapping/layout、二次 matrix/RHS/LU、Single 无 active/单 active/多 active bounds 且所有 inequality 保持源顺序、高层 QP solution evidence、Lajaunie 非一阶重装配失败、错误 source layout/kernel/weight、QP 与非有限普通核 LU 四阶段分类，以及三模型冻结完整 golden。
- 冻结 C++ oracle：用 `g++ 13.3.0 -std=c++11 -O0 -DNDEBUG -fopenmp`、冻结 Eigen 和全部非可视化核心编译被忽略探针；固定 `OMP_NUM_THREADS=1 LC_ALL=C TZ=UTC` 连续两次运行，输出逐字节相同。Single/Lajaunie/Stratigraphic 最终维数分别为 12/9/12，row-major matrix 哈希分别为 `a06d69b3396a05a3`、`02b8296f5a23584c`、`7918b629f414a98c`，RHS 哈希分别为 `bff070d89c79e9b0`、`a8ee910658bcd1fc`、`ec406d431c549468`，LU residual 分别为 `0x1.67c39fea3f324p-51`、`0x1.faf2beb0cd543p-50`、`0x1.ac802174cb041p-49`。最终 probe source、binary、输出 SHA-256 分别为 `71aa129a93ce61b6f1afd573a73539bc735aafaea9732b736e4c55478a7e243f`、`534e9137a3b27dbcbedf5c852721aaa172ee580c32f8cc9333612e4dc1a014c7`、`e42c425e18063720d6c1cc2a3d24d266140c1516acb84c6d7835023a5d74849b`。全核心编译唯一 warning 仍是已登记给 T31 的不可达 Tangent residual selector 漏返回。
- 验证命令：reference HEAD/clean-status；上述 C++ 构建和两次固定环境确定性运行/SHA-256；`cargo fmt --all -- --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all-targets --all-features`；`cargo test --release --test reconstruct`；`cargo test --doc --all-features`；`cargo build --release --all-features`；`RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --all-features`；`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests/test_pure_rust_guard.py`；`python3 tools/audit_pure_rust.py`；`cargo metadata --locked --all-features --format-version 1`；`cargo tree --all-features`；`cargo package --list --locked --allow-dirty`；`cargo package --allow-dirty --locked`；tracked native/ignored oracle、状态连续性和 `git diff --check`。
- 验证结果：全部 T21 门槛通过。严格 Clippy、rustdoc、格式和 release build 无 warning；Cargo 共运行 130 个测试（8 个 T21 与之前 122 个）且全通过，T21 release 测试与 doc test 成功；6 个 Python 护栏测试和实际纯 Rust 审计通过，metadata/tree 仍只有零依赖 `georbf`。发布包清单为 68 个文件，包含 T21 源码和测试、不含 C++/reference/oracle，试打包与从包构建成功（647.1 KiB，压缩后 121.4 KiB）。
- Parity 证据：三模型最终 matrix/RHS 和源/最终 weights 使用冻结 binary64 bit vectors，row-major 哈希 exact；LU 后验 residual 使用 T04 solver 层。两个见证点的 source/final scalar 与 gradient 分别核对冻结 bit vectors：Single 前后字段仅舍入差；Lajaunie/Stratigraphic 因 truncated basis 去除常数项而 scalar 发生同一常数偏移、gradient 保持 T04 容差内一致，测试没有错误要求 scalar 前后相等。特定冻结 Lajaunie source candidate 会被 T20 已定义的 Rust 后验 dual-feasibility 策略拒绝，因此该 conversion golden 从冻结已求得 weights 进入低层 seam；没有削弱 T20 安全门槛。高层 assemble→QP→rebuild→LU 路径由 Single 成功用例覆盖，三模型 conversion/final field 则全部由同组 C++ weights 单独证明。未生成 T32 正式 fixture，也未宣称具体模型端到端或全局 parity。
- 性能证据：重建保持稠密 matrix assembly 的 `O(n²)` 存储/工作和 T18 LU 的 `O(n³)` 主成本，未引入近似或额外 crate；通过 T21 release 专测、release build 和打包构建。未运行 T33 同机 Surfe/GeoRBF 模型 benchmark，未作全局性能判断。
- 后续发现：conversion functions 虽不可达但函数体完整，已按显式操作和兼容文档关闭；Lajaunie 非一阶维数失败与 Stratigraphic stale inequality count 已固定为可观察分支。没有新增任务；下一任务固定为 T22。
