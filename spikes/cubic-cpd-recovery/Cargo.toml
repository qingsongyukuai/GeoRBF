[package]
name = "georbf-cubic-cpd-recovery-spike"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
clarabel = { version = "=0.11.1", default-features = false, features = ["serde"] }
faer = { version = "=0.24.4", default-features = false, features = ["linalg", "std"] }
